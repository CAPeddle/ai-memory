---
phase: 03-node-client-reliable-delivery-regression-safety
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - docs/investigations/ST-084-awcp-host-spike-findings.md
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-03, EVENT-04]
user_setup:
  - service: z2 (Ubuntu execution node, tailnet 100.106.232.78)
    why: "The one-shot criterion-6 evidence must come from the real remote node; no CI runner has tailnet membership."
    env_vars:
      - name: AWCP_NODE_BEARER
        source: "generated locally with `openssl rand -hex 32`, delivered to z2 over ssh into a mode-0600 shell env file outside ~/.awcp/, never echoed and never passed as a command argument (D-16)"
      - name: AWCP_NODE_ENROLMENT_SECRET
        source: "generated locally with `openssl rand -hex 32`; set on the hub via .env + container recreate, and delivered to z2 for exactly one enrolling invocation before being removed (D-11, D-12)"
    dashboard_config:
      - task: "None — every step has a CLI. Access is `ssh personal-server`, NOT a bare `ssh z2`, which matches no alias, offers no key, and fails on publickey."
        location: "~/.ssh/config alias personal-server → Tailscale 100.65.192.115"

estimate:
  tokens: 92000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Replaying the same (node_id, client_seq) event a second time produces no duplicate hub state and the client receives the same ack both times. (ROADMAP Success Criterion 1)"
    - "A node that loses hub connectivity retains bounded local events in ~/.awcp/spool.jsonl (oldest-first) and replays them successfully when connectivity returns. (ROADMAP Success Criterion 2)"
    - "A spool entry is removed only after the hub acknowledges it — not on send, not on retry attempt. (ROADMAP Success Criterion 3)"
    - "When spool capacity is exceeded the oldest event is dropped and a visible counter increments rather than silently filling disk. (ROADMAP Success Criterion 4)"
    - "The durable criterion-6 artifact is the findings write-up — a redacted client transcript plus a committed node_id-scoped SQL readback — not the database rows, which do not survive a docker compose down -v, a machine change, or the DROP SCHEMA the existing suite issues (D-03)."
    - "The enrolment window was opened, used exactly once by a real client driving the real Phase 2 enrolment path, and closed — with closure proven by a further registration attempt returning 401 (D-11)."
    - "Neither the raw node bearer nor the enrolment secret appears anywhere in the committed diff (D-13)."
    - "The scoped readback shows at least one heartbeat row and at least one checkpoint row for z2's node_id, so criterion 6's full definition is discharged rather than its spool-and-replay half."
  artifacts:
    - "docs/investigations/ST-084-awcp-host-spike-findings.md — new `## 16.` section carrying the redacted transcripts, the scoped SQL readback, the closure proof, and the criterion-6 disposition"
    - "server/scripts/awcp-node-client.mjs deployed to z2 as ~/awcp-node-client.mjs (a copy on a different machine — no repo artifact)"
  key_links:
    - "z2 reaches the dev hub at http://100.106.232.78:3000 over the tailnet; docker-compose.yml publishes 3000:3000 on all interfaces while mcp-test is bound to 127.0.0.1:3001 and is unreachable from z2 by construction (D-01)."
    - "Plain http:// is acceptable ONLY because the tailnet path is WireGuard-encrypted end to end. The bearer and the enrolment secret cross this link; any repointing off the tailnet requires TLS (D-01)."
    - "The hub reads AWCP_NODE_ENROLMENT_SECRET from a container environment fixed at creation, so setting it in .env without `docker compose up -d mcp` leaves the running process on the old value — and every failure along this path answers the identical opaque 401 (D-11, RESEARCH.md Pitfall 4)."
    - "Every SQL readback carries WHERE node_id = ... or WHERE bearer_token_hash = ...; an unscoped count over workflow.run_events is nondeterministic the moment a live node streams into the same database (D-02)."
  prohibitions:
    - "[locked] Do NOT simulate disconnection by stopping, restarting, or recreating the dev hub or its database. Experiment 4 is simulated client-side on z2 only (D-18) — the same command family that stops the hub is the one that, with -v, erases this plan's evidence."
    - "[locked] Do NOT run workflow-mvp-e2e.test.ts, or any test, against the dev DATABASE_URL after enrolment — including via the native ./dev.sh inner loop. Its unconditional DROP SCHEMA IF EXISTS workflow CASCADE deletes z2's execution_nodes row and de-enrols the node behind the opaque 401 (D-03)."
    - "[locked] Neither credential may be echoed to a terminal, passed as a command argument, written into ~/.awcp/, or committed. Redact any quoted registration transcript before commit (D-13, D-12)."
    - "[locked] The client must not report a wrong bearer differently from an unenrolled one — the hub answers the same deliberately-opaque 401 for both by design."
    - "[flagged] Do NOT silently adopt the three CONTEXT.md Deferred/Open Questions as scope. See <open_questions>."
---

<objective>
Take the one-shot real-node evidence that discharges ADR-016 criterion 6: enrol z2 through the real
Phase 2 enrolment path, run experiments 4–6 against the dev hub over the tailnet, and commit a redacted
transcript plus a `node_id`-scoped SQL readback into the findings document — at experiment time, before
anything can destroy the rows.

Purpose: `03-CONTEXT.md` D-08 is explicit that the Deno gate and this run are not interchangeable. The
gate guards future changes; only this run answers the question Stage 1 was honest about, which is
whether the topology works against *this host*. And D-03 is equally explicit that the rows are not the
artifact — a `docker compose down -v`, a machine change, or the `DROP SCHEMA` the existing suite issues
all erase them, after which reproducing the run means reopening an enrolment window that D-11 closed on
purpose.

Output: a new `## 16.` section in `docs/investigations/ST-084-awcp-host-spike-findings.md`, and z2
carrying a working, enrolled client.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-node-client-reliable-delivery-regression-safety/03-CONTEXT.md
@.planning/phases/03-node-client-reliable-delivery-regression-safety/03-RESEARCH.md
@.planning/phases/03-node-client-reliable-delivery-regression-safety/03-02-SUMMARY.md
@.planning/phases/03-node-client-reliable-delivery-regression-safety/03-05-SUMMARY.md
@server/scripts/awcp-node-client.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open the enrolment window, enrol z2 once through the real Phase 2 path, close it, and prove closure</name>
  <files>.env (gitignored, not committed) — plus on z2: ~/awcp-node-client.mjs, ~/.awcp-node.env, ~/.awcp/node_id</files>
  <read_first>
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-CONTEXT.md (D-11's six steps in full, D-12, D-13, D-16, and the `## Specific Ideas` note about the `personal-server` alias)
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-RESEARCH.md § Common Pitfalls Pitfall 4 (why "recreate the process" is a step and not a formality)
    - server/src/workflow/remoteNodeHub.ts (the register handler, `ENROLMENT_HEADER` = `X-Node-Enrolment-Secret`, and the docblock's ssh-copy-id model: first registration needs the secret, every later one needs only the bearer)
    - server/src/workflow/store.ts `upsertExecutionNode` (it returns null for an unknown bearer once enrolment is closed — the mechanism that locks a de-enrolled node out behind an indistinguishable 401)
    - docker-compose.yml lines 27-60 (the `mcp` environment block, and the `openssl rand -hex 32` instruction already written into the `AWCP_NODE_ENROLMENT_SECRET` comment)
    - .env.example line 27 (the variable's documented form; it is currently absent from `.env`)
  </read_first>
  <precondition>`ssh personal-server true` succeeds; `ssh personal-server 'node --version'` prints a v18.x version; `ssh personal-server 'curl -s http://100.106.232.78:3000/health'` returns a healthy body; `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{}' http://localhost:3000/workflow/nodes/register` returns 401 (03-01 Task 2 landed); and 03-05 has completed, so no further full-suite run is pending against the dev database.</precondition>
  <reversibility rating="reversible">An env var and two container recreates; no schema or contract change. A scoped `DELETE FROM workflow.execution_nodes WHERE node_id = ...` removes the node at any later point, cascading to its events.</reversibility>
  <action>
    Follow D-11's sequence exactly, with one standing rule layered over all of it: **no command whose
    output is captured may echo either credential.** Both values are generated into mode-0600 files and
    only ever moved by redirection or `$(cat …)` inside a quoted assignment — never printed, never passed
    as a command argument where `ps` or a shell history would capture them.

    1. **Confirm which process serves `:3000`.** `Deno.serve({port: PORT})` binds `0.0.0.0` and a native
       `./dev.sh` serves the same port from `.env.dev`, so a healthy `curl` from z2 does not distinguish
       the containerized `mcp` from a native process. Determine it definitively — e.g. `docker compose ps mcp`
       plus `ss -lntp | grep :3000` — and record the answer, because every later step targets the
       *matching* environment file and the wrong one fails silently.
    2. **Generate the two credentials without printing them.**
       `umask 077; openssl rand -hex 32 > /tmp/awcp-enrol.secret; openssl rand -hex 32 > /tmp/awcp-bearer.hex`
       Also write both values, one per line, to `/tmp/awcp-credentials.list` (mode 0600). That file is the
       input to the mechanical leak check in Task 3 — a `grep -F -f` reads it without ever displaying it.
    3. **Open the window on the hub.** Append the secret to the matching env file with
       `printf 'AWCP_NODE_ENROLMENT_SECRET=%s\n' "$(cat /tmp/awcp-enrol.secret)" >> .env` (or `.env.dev`
       for the native path), then `docker compose up -d mcp` and wait for healthy. **Verify the value
       inside the running process, never infer it from an HTTP response**: `docker compose exec -T mcp printenv AWCP_NODE_ENROLMENT_SECRET | wc -c`
       must print `65` — 64 hex characters plus a newline. Length only; the value stays unseen.
    4. **Deploy the client to z2 and deliver the bearer.**
       `ssh personal-server 'umask 077; cat > ~/awcp-node-client.mjs' < server/scripts/awcp-node-client.mjs`
       then deliver the bearer as a shell env file **outside** `~/.awcp/`:
       `printf 'export AWCP_NODE_BEARER=%s\n' "$(cat /tmp/awcp-bearer.hex)" | ssh personal-server 'umask 077; cat > ~/.awcp-node.env'`
       This satisfies D-16: the *client* reads an environment variable and never a config file, and the
       credential never enters `~/.awcp/`, whose only persisted secret-adjacent content is the `node_id`.
       Piping through stdin keeps the value out of both machines' argv.
    5. **Enrol once.** Deliver the enrolment secret the same way to `~/.awcp-enrol.env`, then run a single
       invocation that sources both, registers, and deletes the secret file:
       `ssh personal-server '. ~/.awcp-node.env; . ~/.awcp-enrol.env; rm -f ~/.awcp-enrol.env; AWCP_HUB_URL=http://100.106.232.78:3000 node ~/awcp-node-client.mjs register'`
       Capture stdout and stderr. Confirm a uuid `node_id` came back, that `~/.awcp` is mode `0700`, that
       `~/.awcp/node_id` is `0600`, and — per D-12 — that no file under `~/.awcp/` contains either
       credential.
    6. **Close the window.** Remove the `AWCP_NODE_ENROLMENT_SECRET` line from the env file, `docker compose up -d mcp`,
       and verify inside the process again: `docker compose exec -T mcp printenv AWCP_NODE_ENROLMENT_SECRET`
       must print an empty value or exit non-zero.
    7. **Prove closure.** Attempt one further registration with a **fresh unknown 64-hex bearer** while
       still presenting the old secret, and assert 401. Build the request with `curl --config <file>`
       (mode 0600) so neither header value reaches argv. This 401 is the closure proof — record it.

    Every failure in this sequence looks identical: the hub answers the same deliberately-opaque 401 for
    a wrong bearer, an unenrolled bearer, and a secret the process never received. A missed recreate is
    undiagnosable from the response, which is why step 3 and step 6 verify inside the process instead.

    Record z2's `node_id` in the SUMMARY, and note that both this task's and Task 2's verify commands
    read it back from `ssh personal-server 'cat ~/.awcp/node_id'` rather than carrying it as a literal.
    The value does not exist until step 5 runs, and a hardcoded placeholder in a `psql` predicate fails
    as an invalid-uuid syntax error rather than as a failed assertion — which reads like a broken hub
    instead of a broken command. Task 1 also writes it to `/tmp/awcp-z2-node-id` so Task 3 can quote it
    without another round trip.
  </action>
  <verify>
    <automated>Z=$(ssh personal-server 'cat ~/.awcp/node_id' | tr -d '[:space:]'); printf '%s' "$Z" > /tmp/awcp-z2-node-id; test -n "$Z" &amp;&amp; test "$(grep -c '^AWCP_NODE_ENROLMENT_SECRET' .env 2>/dev/null || true)" -eq 0 &amp;&amp; test "$(docker compose exec -T mcp printenv AWCP_NODE_ENROLMENT_SECRET | wc -c)" -le 1 &amp;&amp; ssh personal-server 'test "$(stat -c %a ~/.awcp)" = 700 &amp;&amp; test "$(stat -c %a ~/.awcp/node_id)" = 600 &amp;&amp; test ! -e ~/.awcp-enrol.env' &amp;&amp; docker compose exec -T db psql -U ai_memory -d ai_memory -tAc "SELECT count(*) FROM workflow.execution_nodes WHERE node_id = '$Z'" | tr -d '[:space:]' | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - The process serving `:3000` is identified in the SUMMARY as either the containerized `mcp` or a native `./dev.sh`, with the evidence used to decide.
    - Before enrolment, `docker compose exec -T mcp printenv AWCP_NODE_ENROLMENT_SECRET | wc -c` printed `65`.
    - The registration returned HTTP 201 with a uuid `node_id`, and exactly one `workflow.execution_nodes` row exists for that `node_id`.
    - On z2: `~/.awcp` is mode `0700`, `~/.awcp/node_id` is mode `0600`, and concatenating every file under `~/.awcp/` yields content containing neither credential (checked with `grep -F -f` against a mode-0600 list file, never by printing).
    - `~/.awcp-enrol.env` no longer exists on z2.
    - After closure, `docker compose exec -T mcp printenv AWCP_NODE_ENROLMENT_SECRET` yields an empty value or a non-zero exit, and the `.env` file contains no `AWCP_NODE_ENROLMENT_SECRET` line.
    - A registration with a fresh unknown 64-hex bearer plus the old secret returns HTTP `401`, and the request was built via `curl --config` so no header value appeared in argv.
    - No captured command output in this task contains either credential string.
    - `curl -sf http://localhost:3000/health` still returns healthy after both recreates.
  </acceptance_criteria>
  <done>z2 holds an enrolled `node_id`, the enrolment window is closed and proven closed by a 401, and neither credential was echoed, argv-passed, or written into `~/.awcp/`.</done>
</task>

<task type="auto">
  <name>Task 2: Run experiments 4–6 on z2 and capture the evidence at experiment time</name>
  <files>(no repo files — remote node z2; captured transcripts under /tmp for Task 3 to quote)</files>
  <read_first>
    - server/scripts/awcp-node-client.mjs (the full subcommand surface: `register`, `emit`, `checkpoint`, `flush`, `run`, `status`; the exit codes 0/75/77; the env vars `AWCP_HUB_URL`, `AWCP_SPOOL_MAX_ENTRIES`, `AWCP_HEARTBEAT_INTERVAL_MS`)
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-CONTEXT.md (D-18, D-03, D-02, D-13; and the `## Specific Ideas` note that the hub returns 401 for an unknown-but-unenrolled bearer identically to a wrong one)
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-01-SUMMARY.md and 03-02-SUMMARY.md (host-fit friction already recorded during the build — this task adds the operational half)
    - docs/plans/2026-08-04-002-spike-st088-stage2-scope-enforcement-remote-node-plan.md §U4 and line 197 (experiments 4–6 as originally contracted, and criterion 6's full definition: auth, heartbeat, checkpoint, spool, replay, experiments 4–6)
  </read_first>
  <precondition>Task 1 completed: z2 has `~/awcp-node-client.mjs`, `~/.awcp-node.env`, and an enrolled `~/.awcp/node_id`; the enrolment window is closed. No test run against the dev `DATABASE_URL` may occur between now and Task 3's commit.</precondition>
  <action>
    Run the experiments from `ssh personal-server`, capturing stdout and stderr for each into local files
    under `/tmp` for Task 3 to quote. Every hub invocation uses `AWCP_HUB_URL=http://100.106.232.78:3000`.
    Nothing in this task stops, restarts, or recreates the dev hub or its database (D-18).

    **0. Reachability, first.** `ssh personal-server 'curl -s http://100.106.232.78:3000/health'`. STATE.md
    records this verified with no stated expiry; re-running it costs one second and turns a changed
    tailnet link, SSH alias, or `:3000` process into an obvious unreachable host rather than ambiguous
    client behaviour.

    **1. Baseline delivery + heartbeat/checkpoint (criterion 6's other half).** Run
    `AWCP_HEARTBEAT_INTERVAL_MS=5000 node ~/awcp-node-client.mjs run` for roughly 20 seconds, then send
    it `SIGINT`. Expect a start checkpoint, several heartbeats, a stop checkpoint, and an empty spool.
    Without this, the evidence covers spool-and-replay only and criterion 6's definition — which names
    heartbeat and checkpoint explicitly — is left partially undischarged for Phase 4 to discover.

    **2. Experiment 4 — disconnection and replay (EVENT-02, EVENT-03).** Simulate client-side only:
    re-run with `AWCP_HUB_URL` pointed at an unroutable endpoint (e.g. `http://127.0.0.1:1`), emit five
    events, and capture `status` showing them spooled. Confirm the flush reported `deferred` and exited
    `75` with the spool intact. Then restore the correct `AWCP_HUB_URL`, `flush`, and capture the spool
    emptying. Take a copy of `~/.awcp/spool.jsonl` **before** the successful flush — Experiment 5 needs it.

    **3. Experiment 5 — duplicate delivery (EVENT-01).** Restore the pre-flush spool copy over
    `~/.awcp/spool.jsonl` and flush again. The client re-sends already-delivered `(node_id, client_seq)`
    pairs; the hub's `ON CONFLICT DO NOTHING` plus read-back ack must acknowledge them again and add no
    rows. Capture both flush transcripts, and compare the `run_events` row count for this `node_id`
    before and after — it must be unchanged.

    **4. Experiment 6 — invalid authentication (D-17).** Run a flush with a fresh, well-formed but
    unenrolled 64-hex bearer (delivered by the same non-echoing stdin route as Task 1). Expect: exactly
    one request, a structured line naming `auth_failed`, exit code `77`, and the spool intact. Emit one
    event first so the spool is non-empty and "intact" is a claim with content. The client must not
    report this differently from a wrong bearer — the hub refuses to distinguish them and so must the
    client.

    **5. Spool overflow on the real node (EVENT-04).** With `AWCP_SPOOL_MAX_ENTRIES=5` and the hub
    endpoint unroutable, emit eight events. Capture the three structured `reason=spool_overflow` stderr
    lines and a `status` showing `dropped_events=3` and `spooled_events=5`. Restore the endpoint and
    flush, so the five survivors land.

    **6. D-14 on the real node.** After a full drain, emit one more event and confirm from the hub
    readback that its `client_seq` exceeds every previously delivered one. This is the real-node form of
    the in-process restart test — the counter surviving an actual process exit rather than a rebuilt
    config object.

    **7. Scoped SQL readback, immediately.** Against the dev database, and scoped in every query (D-02).
    Bind the node id from z2 rather than pasting it, so the command is copy-runnable and a mistyped uuid
    cannot masquerade as an empty result set:
    `Z=$(ssh personal-server 'cat ~/.awcp/node_id' | tr -d '[:space:]')`
    then
    `docker compose exec -T db psql -U ai_memory -d ai_memory -c "SELECT node_id, hostname, platform, status, first_seen_at, last_seen_at FROM workflow.execution_nodes WHERE node_id = '$Z'"`
    and
    `docker compose exec -T db psql -U ai_memory -d ai_memory -c "SELECT client_seq, event_type, received_at FROM workflow.run_events WHERE node_id = '$Z' ORDER BY client_seq"`.
    Do not select `bearer_token_hash` into anything that will be quoted. Save the output for Task 3.

    Capture now, write up now. The rows do not survive a `docker compose down -v`, a machine change, or
    the `DROP SCHEMA IF EXISTS workflow CASCADE` the existing suite issues — and once they are gone the
    node is de-enrolled behind the opaque 401 and reproducing the run means reopening a window D-11
    deliberately closed.
  </action>
  <verify>
    <automated>Z=$(ssh personal-server 'cat ~/.awcp/node_id' | tr -d '[:space:]'); test -n "$Z" &amp;&amp; docker compose exec -T db psql -U ai_memory -d ai_memory -tAc "SELECT client_seq, event_type, received_at FROM workflow.run_events WHERE node_id = '$Z' ORDER BY client_seq" > /tmp/awcp-readback.txt &amp;&amp; docker compose exec -T db psql -U ai_memory -d ai_memory -tAc "SELECT (count(*) FILTER (WHERE event_type='heartbeat') > 0) AND (count(*) FILTER (WHERE event_type='checkpoint') > 0) AND (count(*) > 10) FROM workflow.run_events WHERE node_id = '$Z'" | tr -d '[:space:]' | grep -qx t &amp;&amp; ssh personal-server '. ~/.awcp-node.env; AWCP_HUB_URL=http://100.106.232.78:3000 node ~/awcp-node-client.mjs status'</automated>
  </verify>
  <acceptance_criteria>
    - The z2 → hub `/health` check succeeded before any experiment ran, and its output is captured.
    - The scoped `run_events` readback for z2's `node_id` contains at least one row with `event_type = 'heartbeat'` and at least one with `event_type = 'checkpoint'`.
    - Experiment 4: with an unroutable endpoint, `status` showed five spooled events and the flush exited `75`; after restoring the endpoint the flush exited `0` and `status` showed zero spooled.
    - Experiment 5: the row count for z2's `node_id` is identical before and after the replay flush, and both flush transcripts show acknowledgements for the same `client_seq` values.
    - Experiment 6: exactly one request was made, the captured stderr contains a line naming `auth_failed`, the exit code was `77`, and `status` afterwards shows the spool still holding the event.
    - Overflow: three structured lines with `reason=spool_overflow` were captured and `status` showed `dropped_events=3` with `spooled_events=5`.
    - D-14: the `client_seq` of the final emitted event, as read back from the hub, is strictly greater than every earlier `client_seq` for that `node_id`.
    - Every SQL statement executed in this task contains `WHERE node_id =` — no unscoped count over `workflow.run_events` or `workflow.execution_nodes` was run.
    - No captured transcript file contains either credential (verified with `grep -F -f /tmp/awcp-credentials.list` over every capture file, expecting no match).
    - The dev hub was not stopped, restarted, or recreated at any point in this task.
  </acceptance_criteria>
  <done>All six ROADMAP-relevant behaviours plus heartbeat and checkpoint are demonstrated on the real node against the real hub, and the transcripts and a `node_id`-scoped readback are captured before anything can destroy them.</done>
</task>

<task type="auto">
  <name>Task 3: Write the criterion-6 findings section, redacted, and prove no credential entered the diff</name>
  <files>docs/investigations/ST-084-awcp-host-spike-findings.md, .planning/STATE.md</files>
  <read_first>
    - docs/investigations/ST-084-awcp-host-spike-findings.md — the section-heading structure (`grep -n '^## ' …`), §12a first per STATE.md, and specifically the existing `## 13.` at line 730 and the second `## 13.` at line 1039 that Phase 1 appended after `## 14` and `## 15`
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-RESEARCH.md § Open Questions #4 (the §13 numbering collision and the recommended `## 16.` heading)
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-CONTEXT.md (D-03, D-13, and the whole `## Deferred / Open Questions` section — three items that must be surfaced, not answered)
    - .planning/phases/03-node-client-reliable-delivery-regression-safety/03-02-SUMMARY.md and 03-03-SUMMARY.md and 03-05-SUMMARY.md (the host-fit friction notes, the repo-rescan open question, and the SAFE-01/02 comparison result)
    - docs/design/adr/ADR-016-awcp-consolidation-host-topology.md §1 (criterion 6's acceptance wording — this section is written against it, and ADR-016 stays Proposed/Conditional until Phase 4)
  </read_first>
  <action>
    Append a new section to `docs/investigations/ST-084-awcp-host-spike-findings.md`, headed
    `## 16. Stage 2 Unit 3: Node Client, Reliable Delivery & Regression Safety (ADR-016 criterion 6)`.

    Use `## 16.` rather than `## 13.` deliberately, and say why in the section's first paragraph: the
    document already carries **two** sections numbered 13 — the Stage 1 "Proposed ADR-016 amendments" at
    line 730 and Phase 1's "Stage 2 Unit 1" appended after `## 14` and `## 15` — so a third would make
    the number useless as a reference. Note that `ROADMAP.md` and `03-CONTEXT.md` both name "§13" for
    this phase's findings and that this heading supersedes those references, using the same supersession-
    note pattern D-04 already used for the `.mjs` filename rather than editing the Tier-1 documents.

    Contents, in this order:

    1. **What was built** — `server/scripts/awcp-node-client.mjs`, its subcommands, its state files, and
       the two test files. One paragraph, with the exit-code contract.
    2. **Enrolment, opened and closed** — D-11's sequence as actually executed, the process that was
       serving `:3000`, and the closure proof 401. **Redact both credentials to a fixed placeholder** in
       any quoted transcript; the single registration most worth quoting is the only one carrying
       `X-Node-Enrolment-Secret`, which is exactly why an unredacted quote would publish the operator's
       secret into git history.
    3. **Experiments 4–6, with transcripts** — each with its captured stdout/stderr, its exit code, and
       the criterion it discharges. Include the heartbeat/checkpoint run and the overflow run.
    4. **Scoped SQL readback** — the two `WHERE node_id = …` queries and their output, verbatim. This is
       the durable artifact: the rows themselves do not survive a `docker compose down -v`, a machine
       change, or the `DROP SCHEMA IF EXISTS workflow CASCADE` the existing suite issues, after which the
       node is de-enrolled behind the opaque 401.
    5. **Criterion-6 disposition** — for each of the six elements criterion 6 names (auth, heartbeat,
       checkpoint, spool, replay, experiments 4–6), state discharged or not, with the evidence pointer.
       **Repo-rescan was not implemented** (see 03-03's open question): record that explicitly as a
       criterion-6 partial rather than leaving Phase 4 to infer it from silence.
    6. **Regression safety** — the SAFE-01 empty-diff result and the SAFE-02 corpus counts from 03-05,
       stated as numbers.
    7. **Open questions carried to Phase 4** — reproduce this plan's `<open_questions>` verbatim. They
       are surfaced, not answered.

    Then run the mechanical leak gate before committing: `git add` the findings file, and confirm that
    `git diff --cached | grep -F -f /tmp/awcp-credentials.list` finds nothing. `grep -F -f` reads the
    credential list from a mode-0600 file without ever displaying it, which makes this check both
    definitive and safe to run in a captured session — strictly better than reading the diff by eye.
    Only after that gate passes, commit. Afterwards `shred -u /tmp/awcp-credentials.list`,
    `/tmp/awcp-enrol.secret` and `/tmp/awcp-bearer.hex`.

    Update `.planning/STATE.md`'s Blockers/Concerns to record that z2 is enrolled, the window is closed,
    and running any test against the dev `DATABASE_URL` will de-enrol it — including via the native
    `./dev.sh` inner loop. That is the standing hazard this phase creates and it outlives the phase.
  </action>
  <verify>
    <automated>grep -n '^## 16\.' docs/investigations/ST-084-awcp-host-spike-findings.md &amp;&amp; git add docs/investigations/ST-084-awcp-host-spike-findings.md &amp;&amp; ! git diff --cached | grep -F -q -f /tmp/awcp-credentials.list</automated>
  </verify>
  <acceptance_criteria>
    - `docs/investigations/ST-084-awcp-host-spike-findings.md` contains exactly one heading matching `^## 16\.` and its first paragraph explains the numbering choice and names the two existing `## 13.` sections by line.
    - The section states that it supersedes the "§13" references in `ROADMAP.md` and `03-CONTEXT.md`, and neither of those Tier-1-adjacent documents was edited to say so.
    - Every quoted transcript that carried a credential shows a fixed redaction placeholder in its place — no truncated prefix, no hash.
    - The section contains both scoped SQL queries verbatim, each containing `WHERE node_id =`, together with their output.
    - The criterion-6 disposition subsection addresses all six named elements individually, and states explicitly that repo-rescan was not implemented.
    - The SAFE-01 result is stated as "empty diff" with the compared file names, and the SAFE-02 corpus `total`/`active` counts are stated as numbers.
    - The `<open_questions>` block below appears in the section, unanswered.
    - `git diff --cached | grep -F -f /tmp/awcp-credentials.list` produces no match before the commit is made.
    - `/tmp/awcp-credentials.list`, `/tmp/awcp-enrol.secret` and `/tmp/awcp-bearer.hex` no longer exist after the commit.
    - `.planning/STATE.md` records the standing de-enrolment hazard.
  </acceptance_criteria>
  <done>The criterion-6 evidence is committed as a redacted, numbered findings section with a scoped SQL readback, proven credential-free by a mechanical check, and the standing de-enrolment hazard is on the record.</done>
</task>

</tasks>

<open_questions>
These are `03-CONTEXT.md`'s own `## Deferred / Open Questions`, deliberately surfaced rather than
adopted — each adds a work obligation the discuss-phase interview never put to the user. Reproduce them
in the `## 16.` findings section unanswered; do not silently do the work, and do not silently skip it.

**1. Nothing in this phase observes memory tools and a live node on the same stack (product-lens, P1).**
Criterion 6 asks whether the topology works *against this host* — a co-tenancy claim — yet regression
evidence comes from the test stack while the only real node writes to the dev stack. The phase can ship
every criterion green and leave co-tenancy untested, and Phase 4 must then reconcile its host
recommendation without any observation of the memory MCP under a real node's traffic.
*Proposal (unapproved, ~10 minutes):* immediately after the z2 run, exercise one authenticated
`search_thoughts` and one `capture_thought` against the same stack the node streamed into, and record
the result in `## 16.` as the co-tenancy observation. **This plan does not do it.** If the user wants
it, it is one added step in Task 2.

**2. Host-fit friction discovered here is never routed to criterion 7 (product-lens, P2).**
Phase 4 must answer whether inheriting this codebase costs less than it saves. D-04/D-05 already record
a concrete inheritance cost — no `package.json` anywhere, so the first Node artifact must be `.mjs`, and
the obvious remedy would change what npm tooling infers about a Deno-only tree — and 03-02/03-05
recorded more of it in their SUMMARYs (the Deno `node:` import behaviour, the permission-grant
expansion). Because Phase 3's findings section is scoped to experiment results, that material has no
route into criterion 7 and gets written from recall later. *Proposal (unapproved, ~15 minutes):* add a
short "host-fit friction observed while building" subsection to `## 16.`, sourced from the plan
SUMMARYs. **This plan does not add it**, but the raw material exists and is cited above so the decision
is still live rather than lost.

**3. Does `FEATURE_WORKFLOW` stay enabled on the base `mcp` service after Phase 3?** (carried from
03-01.) Leaving it on makes `/api/workflow/*` and an unauthenticated `/workflow` dashboard shell part of
every `docker compose up -d` on a port published on all interfaces; turning it off silently re-breaks
future real-node work with a 404. A maintainer decision either way.

**4. Is repo-rescan in Phase 3 scope?** (carried from 03-03.) The canonical plan lists it under U3 and
`03-CONTEXT.md` leaves its membership explicitly open. It was not implemented. Task 3 records that as a
criterion-6 partial; whether it should have been built is still unanswered.
</open_questions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| z2 → dev hub `:3000` over the tailnet | Raw bearer, and once the enrolment secret, cross this link in plain `http://`. Acceptable only because WireGuard encrypts it end to end. |
| operator session → captured transcripts → committed findings document → git history | This phase deliberately captures output as evidence and commits it. A leak here is permanent and public to every future clone. |
| enrolment window, open | While open, any 64-hex bearer presenting the secret can enrol. There is no per-node revocation: `store.ts:677-679` records that deleting a node's row lets the same secret re-enrol it, and `status` has no `revoked` value. |
| dev database ↔ the existing test suite | The suite's `DROP SCHEMA IF EXISTS workflow CASCADE` targets whatever `DATABASE_URL` resolves to, which the native dev inner loop points at the dev database. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | either credential entering the committed findings document or git history | critical | mitigate | Redaction to a fixed placeholder in every quoted transcript, plus a mechanical pre-commit gate: `git diff --cached \| grep -F -f /tmp/awcp-credentials.list` must find nothing. `grep -F -f` reads the list without displaying it, so the check is safe to run inside a captured session. Credential files are `shred -u`'d afterwards. |
| T-03-06-02 | Information Disclosure | either credential in argv, shell history, or a captured command echo | high | mitigate | Both values are generated straight into mode-0600 files and moved only by stdin redirection or `$(cat …)` inside a quoted assignment. Header values reach `curl` via `--config`, never `-H`. Verification of the hub-side secret is by `printenv … \| wc -c` (length only). |
| T-03-06-03 | Spoofing | the enrolment window left open, allowing any bearer holding the secret to enrol | high | mitigate | D-11's close-and-prove sequence: remove the value, recreate the process, verify inside the process, then attempt a registration with a fresh unknown bearer and require 401. The proof is a positive test, not an assumption that the removal took effect. |
| T-03-06-04 | Elevation of Privilege | no per-node revocation exists | medium | accept | Documented and accepted for one operator-provisioned node with the window closed (`store.ts:677-679`). It becomes a real gap the moment a second node is added; record that in `## 16.` as input to Phase 4 rather than treating it as closed. |
| T-03-06-05 | Denial of Service | the evidence rows destroyed and z2 de-enrolled by a later test run | high | mitigate | Evidence is captured and written up at experiment time (D-03). The wave ordering puts the last full-suite run in 03-05, before enrolment. Task 3 records the standing hazard in `.planning/STATE.md` because it outlives the phase. |
| T-03-06-06 | Tampering | disconnection simulated by stopping the hub or its database | high | mitigate | Prohibited in frontmatter and enforced by construction: experiment 4 repoints `AWCP_HUB_URL` at an unroutable endpoint on z2. Task 2's acceptance criteria require that the dev hub was never stopped, restarted, or recreated. |
| T-03-06-07 | Information Disclosure | plain `http://` carrying credentials | medium | transfer | Transferred to the tailnet's WireGuard encryption, which is the actual control (D-01). Any repointing of the client off the tailnet requires TLS — state that constraint in `## 16.` so a future reader who moves the endpoint inherits it. |
| T-03-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs on either machine; the client is a single file using Node built-ins (D-07), copied to z2 with `cat`. |
</threat_model>

<verification>
1. `docker compose exec -T mcp printenv AWCP_NODE_ENROLMENT_SECRET` yields empty or non-zero — the window is closed.
2. A registration with a fresh unknown 64-hex bearer plus the old secret returns `401`.
3. The scoped `run_events` readback for z2's `node_id` contains at least one `heartbeat` row and at least one `checkpoint` row.
4. `grep -n '^## 16\.' docs/investigations/ST-084-awcp-host-spike-findings.md` matches exactly one line.
5. `git diff --cached | grep -F -f /tmp/awcp-credentials.list` produces no match prior to the commit.
6. The dev hub was never stopped, restarted, or recreated during Task 2.

<human-check>
Read the `## 16.` section end to end before it is pushed, specifically the quoted transcripts. The
mechanical gate proves the two known credential strings are absent; it cannot judge whether a quoted
line reveals something else worth withholding, nor whether the criterion-6 disposition is an honest
account rather than a favourable one. This is the one artifact in the phase that a future reader will
treat as the evidence itself.
</human-check>
</verification>

<success_criteria>
- z2 enrolled through the real Phase 2 path, and the window closed with a positive closure proof.
- Experiments 4–6, plus heartbeat, checkpoint, overflow and the D-14 counter, demonstrated on the real node against the real hub.
- The evidence is durable: a redacted transcript and a `node_id`-scoped SQL readback committed at experiment time, not rows in a volume.
- No credential in the diff, proven mechanically rather than by eye.
- Every criterion-6 element has a stated disposition, including the one that was not built.
</success_criteria>

## Artifacts this phase produces

**Created or modified by this plan**
- `docs/investigations/ST-084-awcp-host-spike-findings.md` — new `## 16. Stage 2 Unit 3: Node Client, Reliable Delivery & Regression Safety (ADR-016 criterion 6)` section.
- `.planning/STATE.md` — standing de-enrolment hazard recorded in Blockers/Concerns.
- On z2 (not repo artifacts): `~/awcp-node-client.mjs`, `~/.awcp-node.env` (mode 0600, outside `~/.awcp/`), `~/.awcp/` (0700) with `node_id`, `client_seq`, `spool.jsonl`, `state.json` (each 0600).
- In the dev database: one `workflow.execution_nodes` row and its `workflow.run_events` rows for z2's `node_id`.

**Consumed from earlier plans** — the complete client (03-02..03-04), the `FEATURE_WORKFLOW` enablement (03-01), and the SAFE-01/02 results (03-05).

<output>
Create `.planning/phases/03-node-client-reliable-delivery-regression-safety/03-06-SUMMARY.md` when done.
Commit each task separately with a Conventional Commit carrying a `Story: ST-088` trailer. The Task 3
commit must not be made until the mechanical credential gate passes.
</output>
