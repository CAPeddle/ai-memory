---
phase: 02-remote-node-identity-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db/workflow/003_execution_nodes.sql
  - server/db/workflow/004_run_events.sql
  - server/src/workflow/store.ts
  - server/src/workflow/remoteNodeHub.ts
  - server/index.ts
  - server/tests/workflow-remote-node-hub.test.ts
  - server/tests/workflow-migrations.test.ts
  - server/tests/workflow-mvp-e2e.test.ts
autonomous: false
requirements: [NODE-01, NODE-02]
user_setup: []

must_haves:
  truths:
    - "A POST to /workflow/nodes/register with a per-node bearer in the Authorization header persists exactly one workflow.execution_nodes row keyed by the SHA-256 digest of that bearer and returns its node_id (U2 register contract)."
    - "A second register call with the same bearer returns the SAME node_id and does not create a second row (idempotent upsert — resolves the NODE-01 repeat-registration probe)."
    - "Two simultaneous register calls with the same bearer resolve to a single node identity, guaranteed by the UNIQUE(bearer_token_hash) constraint (resolves the NODE-01 concurrency probe)."
    - "A POST to /workflow/nodes/:node_id/events for an existing node records each submitted event in workflow.run_events attributed to that node_id and returns an acknowledgement entry for every submitted client_seq (U2 events contract)."
    - "Re-submitting an identical (node_id, client_seq) event inserts no second row (ON CONFLICT DO NOTHING) and the acknowledgement still covers that client_seq (idempotent, ownership-bound event batches)."
    - "Only the bearer digest is ever persisted; the raw bearer is never stored, never logged, and never returned in any response body (U2 never-store-raw-bearer)."
    - "A register call whose bearer does not match the required 32-byte / 64-lowercase-hex format (^[0-9a-f]{64}$) is rejected with 401 before any hashing or persistence — the deterministic SHA-256 credential store is only sound for a high-entropy bearer (Option A, bearer_input_contract)."
  artifacts:
    - server/db/workflow/003_execution_nodes.sql
    - server/db/workflow/004_run_events.sql
    - "server/src/workflow/store.ts (node persistence functions added)"
    - server/src/workflow/remoteNodeHub.ts
    - "server/index.ts (node hub routes mounted inside the `if (workflowFeatureEnabled())` block at :1174)"
    - server/tests/workflow-remote-node-hub.test.ts
    - "server/tests/workflow-migrations.test.ts (migration-set expected values updated 001..004)"
    - "server/tests/workflow-mvp-e2e.test.ts (migration-set expected values updated 001..004)"
  key_links:
    - "Migration runner (server/src/workflow/schema.ts runWorkflowMigrations) auto-discovers 003 and 004 by the ^(\\d+)_.*\\.sql$ pattern — no runner change."
    - "remoteNodeHub.ts route handlers call store.ts node functions (NOT ../db.ts) — required by workflow-boundary.test.ts 'only store.ts holds the database handle'."
    - "index.ts mounts app.route('/workflow/nodes', createRemoteNodeHubRoutes()) inside `if (workflowFeatureEnabled())` at server/index.ts:1174 — the HTTP mount seam, alongside the existing /api/workflow route at :1233. NOT the `if (workflowBootstrap.enabled)` block at :74, which is module top-level and runs 1000+ lines before `const app = new Hono()` at :1132. Both predicates derive from FEATURE_WORKFLOW=true, so absence of a node still changes nothing about boot."
    - "Adding 003/004 changes the discovered migration set from [1,2] to [1,2,3,4], which BREAKS the hardcoded expected values in the two REAL-directory tests of workflow-migrations.test.ts (:438, :439-440, :457, :460, :461-465) and in workflow-mvp-e2e.test.ts (:127, :128-131, :141-144, :562-566). Task 2 owns updating them. The hermetic synthetic tests at :124/:145/:150 must NOT be touched — see <migration_set_fanout>."
  prohibitions:
    - "[flagged] No general-purpose remote shell or arbitrary control surface is introduced — only /workflow/nodes/register and /workflow/nodes/:node_id/events exist; the §7.1 allow-listed control messages are NOT dispatched to nodes in this phase (hub-only)."
    - "[flagged] No endpoint exposes another node's identity or events — there is no node-list, no cross-node read, and no events-read endpoint in this phase."
    - "[flagged] No raw-bearer recovery path exists — registration accepts a pre-provisioned bearer and returns only node_id; no endpoint returns, mints, or recovers a raw bearer."
---

<objective>
Deliver the Phase 2 tracer: a production-quality, end-to-end remote-node registration and
event-ingestion slice on the hub, wired from SQL migration through the workflow store, the
Hono route factory, and the composition root — proven by an in-process integration test.

This plan operationalizes the U2 "Hub-side remote-node tables and auth endpoint" contract
(hub only; no node client). It creates the two deferred tables (`workflow.execution_nodes`,
`workflow.run_events`), adds their persistence functions to `store.ts`, exposes the two U2
endpoints via a new `remoteNodeHub.ts` factory, and mounts them inside the optional workflow
boot block.

Purpose: prove the whole register→persist→ingest→acknowledge path works on one happy path
before the security/regression expansion (Plan 02) hardens the rejection and isolation edges.
Output: migrations 003/004, node store functions, `remoteNodeHub.ts`, an index.ts mount, and
a failing-first integration suite that turns green.

**User story (derived from the ROADMAP Phase 2 Goal line — see note in return):**
**As a** hub operator provisioning an authorized Ubuntu execution node,
**I want to** register that node with its own per-node bearer and ingest its execution events
idempotently, **so that** the hub durably attributes every event to the correct node for later
inspection without any impact on platform MCP authentication.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remote-node-identity-hub/02-RESEARCH.md
@.planning/phases/02-remote-node-identity-hub/02-PATTERNS.md
@.planning/phases/02-remote-node-identity-hub/02-VALIDATION.md
@docs/plans/2026-08-04-002-spike-st088-stage2-scope-enforcement-remote-node-plan.md
@docs/investigations/ST-084-awcp-host-spike-findings.md
</context>

<research_corrections>
**Two corrections to 02-RESEARCH.md / 02-PATTERNS.md — the executor MUST follow these, not the
research code samples, where they conflict. Verified against the live boundary test.**

1. **All node SQL lives in `store.ts`, NOT in `remoteNodeHub.ts`.** `server/tests/workflow-boundary.test.ts`
   ("boundary: only store.ts holds the database handle", ~line 221) asserts that every workflow
   file except `store.ts` and `schema.ts` must NOT import `../db.ts`. The research samples that
   import `sql` from `../db.ts` inside `remoteNodeHub.ts` would fail this enforced test. Add the
   node persistence functions to `store.ts` (which already holds the `sql` handle) and have
   `remoteNodeHub.ts` call them. `remoteNodeHub.ts` imports only `./store.ts`, `./api.ts`
   (`toHttpError`), package/`node:` specifiers, and the `crypto.subtle` global.

2. **Do NOT use `ON CONFLICT ... DO UPDATE SET` in `store.ts`.** The boundary test's
   schema-qualification regex (`\b(?:FROM|INTO|UPDATE|JOIN)\s+([A-Za-z_][\w.]*)`) captures the token
   after `UPDATE`; on `DO UPDATE SET last_seen_at` it captures `SET` and flags it as an unqualified
   identifier, failing the test. Implement the register upsert as UPDATE-then-INSERT-ON-CONFLICT-DO-NOTHING
   inside a single `sql.begin` (see Task 3). `api.ts` exports `toHttpError` but NOT `withErrorMapping`
   or `readJson` — define a local error wrapper / JSON reader in `remoteNodeHub.ts`.
</research_corrections>

<locked_contract>
**U2 register contract (pre-provisioned bearer — do NOT invent mint/return semantics).**
Per the canonical plan (`docs/plans/...` §U2) and §7.1 of the findings: each node carries its own
per-node bearer in its own env var (the Phase-3 client supplies it). Registration *validates and
upserts*; it returns `node_id`. Registration therefore:
- Validates the presented `Authorization: Bearer <token>` header matches the required bearer format
  (`^[0-9a-f]{64}$`, see <bearer_input_contract>) and, if not, rejects with 401 before hashing.
- Computes the deterministic `sha256Hex(rawBearer)` (64-char lowercase hex) — the **Option A** design
  confirmed at Task 1.
- Upserts the node keyed by `bearer_token_hash` (UNIQUE), returning the existing-or-new `node_id`.
- Returns `{ "node_id": "<uuid>" }` with HTTP 201. It NEVER returns a raw or minted bearer.

This deterministic single-stored-column upsert is only valid under **Option A**; **Option B** (bcrypt)
is incompatible with a single `bearer_token_hash` column and triggers the schema replan described in
Task 1 (it is out of scope for the execution path below).

**U2 events contract.** `/workflow/nodes/:node_id/events` bulk-inserts with
`ON CONFLICT (node_id, client_seq) DO NOTHING` and returns an acknowledgement covering every
submitted `client_seq` (both freshly inserted and already-present). The full ownership binding and
auth-rejection edges are hardened in Plan 02; this plan wires the happy path and the duplicate-ack
path.
</locked_contract>

<bearer_input_contract>
**Pre-provisioned bearer: entropy & format requirements (Option A execution path).**
The deterministic SHA-256 digest is a sound credential store ONLY because the per-node bearer is a
high-entropy machine secret (at least 128 bits), never a human-chosen password. This plan REQUIRES,
and Task 2 must test, a concrete supported bearer format so the entropy floor is enforceable rather
than assumed:
- **Format:** the raw bearer MUST be exactly 32 random bytes encoded as 64 lowercase hex characters,
  matching `^[0-9a-f]{64}$` (256 bits of entropy — comfortably above the 128-bit floor).
- **Generation (operational, documented):** provision each node bearer with `openssl rand -hex 32`
  (equivalently `python3 -c "import secrets; print(secrets.token_hex(32))"`).
- **Enforcement:** validateNodeBearer / register MUST reject any bearer that does not match
  `^[0-9a-f]{64}$` with 401 BEFORE hashing or persisting — a malformed or short bearer is not a valid
  node credential. This is a format/length floor that guarantees the minimum ENCODED entropy; it is
  NOT a claim that length alone implies randomness (the generation command supplies the randomness).
- Under **Option B** this contract is replaced by the fingerprint+verifier schema and is out of scope
  for this plan.
</bearer_input_contract>

<migration_set_fanout>
**Landing 003/004 breaks seven pre-existing assertions. Task 2 owns fixing them.**

Two suites hardcode the discovered migration set as exactly `[1, 2]`. They are correct today and
must be updated to `[1, 2, 3, 4]` the moment 003/004 exist — otherwise Wave 1's own wave command
fails, and Plan 02's full-suite phase gate can never go green.

**`server/tests/workflow-migrations.test.ts` — the two REAL-directory tests only:**
- `:432` — test NAME says "discovers 001 and 002 in order"; rename to 001..004
- `:438` — `assertEquals(found.map((m) => m.version), [1, 2], "versions in ascending order")`
- `:439-440` — `found[0]` / `found[1]` filename assertions (add `found[2]`, `found[3]`)
- `:457` — `assertEquals(report.skipped.map((s) => s.version), [1, 2])`
- `:460` — `assertEquals(ledger.map((r) => r.version), [1, 2])`
- `:461-465` — ledger filename list

**DO NOT TOUCH `:124`, `:145`, `:150` in that same file.** They also read `[1, 2]`, but they belong
to the HERMETIC tests driving synthetic in-memory migrations named `001_alpha.sql` / `002_beta.sql`
against a scratch schema. They are unaffected by anything in `server/db/workflow/`, and "fixing"
them to `[1,2,3,4]` breaks tests that were correct. Match on the surrounding migration FILENAMES,
not on the `[1, 2]` literal, before editing any site.

**`server/tests/workflow-mvp-e2e.test.ts`:**
- `:127` — ledger versions
- `:128-131` — ledger filename list
- `:141-144` — `/ready` `checks.workflow.applied`
- `:562-566` — second-boot `checks.workflow.skipped`

(`:135`, an `assertStringIncludes` on `001_workflow_schema.sql` only, survives unchanged.)

These are **expected-value updates, not weakened assertions**. Preserve every assertion's shape:
exact ordered `assertEquals` on full lists. Do NOT convert any of them to `.includes`, a length
check, or a subset match — the exactness is the point, and relaxing it would silently permit a
future migration to go unnoticed. No other behaviour in either file changes.
</migration_set_fanout>

<schema_gate>
This plan changes persistent schema. Executor MUST honor:
- **Migration numbers:** `003_execution_nodes.sql`, `004_run_events.sql` — verified next in sequence
  after 001/002.
- **No `IF NOT EXISTS`** on any table, index, or constraint in 003/004 (deliberate break from 001;
  002 documents the rationale). The `workflow.schema_migrations` ledger owns idempotency and drift
  detection.
- **Two DISTINCT idempotency mechanisms — do not fuse them.** Through the ledger, re-application is
  a **silent skip**, and that is the asserted, correct behaviour: `workflow-migrations.test.ts:453-456`
  runs `runWorkflowMigrations()` twice and asserts `report.applied === []` with
  `report.skipped === [1,2]`. What `NO IF NOT EXISTS` buys is a **loud failure on raw DDL
  re-execution that bypasses the ledger** (`tx.unsafe(migration)` on an already-migrated schema).
  A test that expects `runWorkflowMigrations()` to throw on a second call will find no throw —
  assert the skip, not an error.
- **Forward behavior:** the runner applies each file in its own transaction under advisory lock
  `840_084`, writing the ledger row inside the same transaction; a checksum drift on an applied
  migration aborts before any pending migration runs.
- **Existing-database upgrade:** a database already at 002 must apply 003 then 004 cleanly and leave
  001/002 checksums unchanged (verified by the migration-application test in Task 2).
- **Rollback / recovery posture:** teardown remains `DROP SCHEMA workflow CASCADE`; both tables live
  in the `workflow` schema and `run_events.node_id` is `ON DELETE CASCADE`, so no orphaned rows and
  no shared-chain bookkeeping to unwind.
- **Data-contract invariants:** `status IN ('active','paused','offline')`; `bearer_token_hash` is
  `NOT NULL UNIQUE`; `run_events(node_id, client_seq)` is UNIQUE; `run_events.node_id` FK →
  `execution_nodes(node_id)`; every object is `workflow.`-schema-qualified.
</schema_gate>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the persisted bearer-digest format (one-way — fixes on-disk credential format)</name>
  <reversibility rating="one-way">The chosen design defines the on-disk value AND the identity
  schema of workflow.execution_nodes. Option A (deterministic SHA-256) fixes
  bearer_token_hash and, once any node registers, changing it requires re-provisioning every node
  bearer and truncating the table. Option B is NOT a hashing swap: salted bcrypt cannot serve the
  canonical single-column deterministic upsert, so it demands a different data contract (a separate
  UNIQUE deterministic fingerprint column plus a slow verifier hash column) plus a pinned dependency
  and a frozen-lock refresh — i.e. a schema/plan replan, not an in-plan branch. This is a door the
  agent must not walk through unattended.</reversibility>
  <decision>Adopt the deterministic single-column SHA-256 credential design (Option A, executable
  now), or stop and replan for a bcrypt fingerprint+verifier schema (Option B)?</decision>
  <context>
  The canonical U2 text names bcrypt, but two facts conflict with a drop-in bcrypt: (1) the frozen
  `server/deno.json` contains only hono, @hono/mcp, @modelcontextprotocol/sdk, and zod — bcrypt is
  absent from deno.json and deno.lock, and `"lock": { "frozen": true }` aborts CI on any un-locked
  import; and (2) the canonical U2 data contract uses a SINGLE stored `UNIQUE(bearer_token_hash)`
  column for deterministic identity/upsert lookup by SQL equality, which salted bcrypt cannot satisfy
  — hashing the same bearer twice yields different values, and SQL equality cannot verify a bcrypt
  hash without scanning candidate rows. Bcrypt is therefore not a hashing swap but a schema change.
  This conflict must not be resolved silently.
  </context>
  <options>
    <option id="option-a">
      <name>A — CONFIRM the deterministic dependency-free SHA-256 digest + node:crypto.timingSafeEqual (RECOMMENDED, EXECUTABLE NOW)</name>
      <pros>No deno.json/deno.lock change; reuses the exact crypto.subtle SHA-256 pattern already in
      schema.ts checksumOf(); timing-safe comparison via node:crypto timingSafeEqual (Deno
      Node-compat, no lock entry). DETERMINISTIC: the same bearer always yields the same 64-char
      lowercase-hex digest, so one stored `UNIQUE(bearer_token_hash)` column supports the U2
      idempotent identity/upsert lookup by plain SQL equality. This is the ONLY option executable
      under the current canonical single-column schema; selecting A confirms Tasks 2 and 3 proceed
      exactly as written.</pros>
      <cons>SHA-256 is a fast digest, not a slow password KDF. This is acceptable ONLY because the
      per-node bearer is a pre-provisioned HIGH-ENTROPY machine secret (at least 128 bits) — never a
      human-chosen password — so brute-forcing the digest is infeasible and a slow KDF buys nothing.
      The suitability claim is CONDITIONAL on <bearer_input_contract>: the bearer MUST be 32 random
      bytes encoded as 64 lowercase hex characters, generated with the documented command, and
      malformed/low-entropy-format input MUST be rejected (tested in Task 2).</cons>
    </option>
    <option id="option-b">
      <name>B — STOP and replan: bcrypt fingerprint+verifier schema expansion (NOT EXECUTABLE under this plan)</name>
      <pros>Matches the literal canonical U2 wording (slow KDF); salted bcrypt hardens the stored
      credential even against a low-entropy bearer.</pros>
      <cons>NOT EXECUTABLE under this plan or the current canonical single-column schema. Because
      salted bcrypt is non-deterministic, `UNIQUE(bearer_token_hash)` can no longer serve the
      deterministic identity/upsert lookup. A viable bcrypt design REQUIRES a data-contract change:
      add a separate `bearer_token_fingerprint text NOT NULL UNIQUE` column (deterministic, e.g.
      sha256Hex, for the O(1) identity lookup) PLUS a slow `bearer_verifier_hash` bcrypt column (to
      verify the presented bearer against the located row), rewriting 003's canonical fields and
      migrations; and it adds a PO-approved pinned bcrypt dependency with a deliberate deno.lock
      refresh and a package-legitimacy verification of `npm:bcryptjs` (currently [ASSUMED],
      unverified). Selecting B therefore HALTS execution BEFORE Task 2 — the executor records B, does
      NOT run Tasks 2/3, and returns a replan requirement for the revised schema/plan.</cons>
    </option>
  </options>
  <resume-signal>Record the decision explicitly — NO silent default:
  - **option-a**: record `A` in the SUMMARY and CONTINUE to Task 2 (deterministic SHA-256,
    single-column schema, as written).
  - **option-b**: record `B` in the SUMMARY and STOP — do NOT execute Task 2 or Task 3; return a
    replan requirement for the `bearer_token_fingerprint` (UNIQUE, deterministic) + `bearer_verifier_hash`
    (bcrypt) schema expansion, pinned dependency, and lock refresh. B is not executable under the
    current plan.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — author the failing hub integration suite and the two schema migrations</name>
  <precondition>Task 1 recorded decision **A** (deterministic SHA-256, single-column schema). If Task 1
  recorded **B**, HALT: this task and Task 3 are not executable under the current canonical schema and
  require a replan (see Task 1).</precondition>
  <read_first>
    - server/tests/workflow-migrations.test.ts (withScratchSchema helper ~L50-63; `const T = { sanitizeResources: false, sanitizeOps: false }` L3)
    - server/tests/workflow-agent-key-e2e.test.ts (auth assertion + discrimination-control structure)
    - server/db/workflow/001_workflow_schema.sql (column/CHECK/index DDL style; note: 001 uses IF NOT EXISTS — do NOT copy that)
    - server/db/workflow/002_decision_run_packet_integrity.sql (header comment style; no-IF-NOT-EXISTS rationale, L1-50)
    - server/src/workflow/schema.ts (runWorkflowMigrations discovery + checksumOf SHA-256, L72-80, L228-235)
    - .planning/phases/02-remote-node-identity-hub/02-VALIDATION.md (Per-Task Verification Map, Red Controls)
    - <bearer_input_contract> above (bearer format/entropy floor and the malformed-input RED control)
  </read_first>
  <behavior>
    Failing-first integration tests in server/tests/workflow-remote-node-hub.test.ts, driving
    createRemoteNodeHubRoutes() in-process via app.fetch, each isolated in a unique
    withScratchSchema("test_hub_NNN", ...) schema WHERE an isolated ledger is genuinely needed —
    see the migration-discovery bullet below for why the real 003/004 files are NOT applied that
    way. Tests to author (RED where the route/behavior does not yet exist):
    - migration-discovery + shape (GREEN once 003/004 written). **Do NOT apply the real 003/004
      files "into a scratch schema" — that is structurally impossible.** `applyMigrations`
      (schema.ts:237-249) uses its `schemaName` option ONLY for `CREATE SCHEMA IF NOT EXISTS` and
      the ledger table; the migration statements are applied verbatim, and <schema_gate> requires
      every object in 003/004 to be hardcoded `workflow.`-qualified. Real DDL "applied into
      test_hub_042" therefore lands in the SHARED `workflow` schema, the `finally` drop cleans up
      nothing, and — with no `IF NOT EXISTS` — the second run of the suite dies on
      `relation "execution_nodes" already exists` against `db-test`, which CLAUDE.md documents as
      shared and accumulating (wiped only when the container stops). `withScratchSchema` exists for
      SYNTHETIC in-memory migrations (see workflow-migrations.test.ts:9-25); discovery against the
      real directory is proven separately. Follow that split:
        * discovery: `discoverMigrations()` returns versions [1,2,3,4] in ascending order with the
          expected filenames and 64-char SHA-256 checksums (mirror workflow-migrations.test.ts:432).
        * shape: after `runWorkflowMigrations()`, query `information_schema` / `pg_constraint`
          against the real `workflow` schema for the documented execution_nodes and run_events
          columns, the status CHECK, UNIQUE(bearer_token_hash), and UNIQUE(node_id, client_seq).
        * ledger idempotency: a second `runWorkflowMigrations()` applies nothing —
          `report.applied === []`, `report.skipped === [1,2,3,4]` (mirror :456). This is a SKIP,
          not a throw; see <schema_gate>.
      Reserve `withScratchSchema("test_hub_NNN", ...)` for anything needing an isolated ledger, and
      drive it with synthetic DDL only.
    - NODE-01a register happy path: valid bearer → 201 with a uuid node_id (RED: route absent).
    - NODE-01b idempotent upsert: second register with the same bearer → 201, SAME node_id, exactly
      one row (RED).
    - NODE-01 concurrency: two concurrent register calls with the same bearer resolve to one row /
      one node_id (RED) — asserts the UNIQUE(bearer_token_hash) guarantee.
    - NODE-02a events happy path: register, then POST one event to the returned node_id → 200, one
      row in workflow.run_events attributed to that node_id (RED).
    - NODE-02b duplicate replay (RED control #3): POST the same (node_id, client_seq) twice → 200 both
      times, exactly one row, acknowledgement covers the client_seq both times.
    - NODE-02c ack completeness: a batch of N events returns an acknowledgement entry for every
      submitted client_seq.
    - raw-bearer never persisted (RED control): after registration, the raw bearer string does not
      appear in any workflow.execution_nodes column; only a 64-char hex digest is stored.
    - bearer-format rejection (RED control): a register call with a bearer that does NOT match
      `^[0-9a-f]{64}$` (e.g. "short", uppercase hex, or non-hex) → 401 and zero rows in
      workflow.execution_nodes; the documented valid form is `openssl rand -hex 32` output. This
      enforces the high-entropy floor that makes the deterministic SHA-256 store sound
      (<bearer_input_contract>).
    - unique-constraint red control #5: a helper/asserted variant proves that WITHOUT
      UNIQUE(node_id, client_seq) the duplicate-row assertion would fail — encode as a comment-guarded
      assertion on the constraint's presence in the scratch schema (do not drop the production constraint).
    Positive controls must establish that each RED failure is for the intended reason (missing route
    / missing table), not an unrelated error — assert on status codes and row counts, not on thrown
    stack traces.
    Also write the two migration files so the migration-apply test can pass:
    - 003_execution_nodes.sql: node_id uuid PK default gen_random_uuid(); bearer_token_hash text NOT
      NULL UNIQUE; registered_at/last_seen_at timestamptz NOT NULL DEFAULT now(); status text NOT NULL
      DEFAULT 'active' CHECK (status IN ('active','paused','offline')); hostname text; platform text;
      plus index idx_workflow_nodes_status (status, registered_at DESC). Header comment mirrors 002.
      Every object workflow.-qualified. NO IF NOT EXISTS.
    - 004_run_events.sql: event_id uuid PK default gen_random_uuid(); node_id uuid NOT NULL REFERENCES
      workflow.execution_nodes(node_id) ON DELETE CASCADE; client_seq bigint NOT NULL; event_type text
      NOT NULL; payload jsonb; received_at timestamptz NOT NULL DEFAULT now(); CREATE UNIQUE INDEX
      uq_run_events_node_seq ON workflow.run_events (node_id, client_seq); supporting index
      idx_workflow_run_events_node (node_id, received_at DESC). NO IF NOT EXISTS.
    Finally, absorb the migration-set fan-out described in <migration_set_fanout> — landing 003/004
    without it leaves seven pre-existing assertions red for a reason unrelated to this plan.
  </behavior>
  <action>
    Create server/tests/workflow-remote-node-hub.test.ts and the two migration files per the behavior
    block, following the read_first analogs exactly. Copy `const T` and withScratchSchema verbatim;
    prefix every scratch schema with test_hub_ and drop it in finally; never drop the production
    `workflow` schema. Drive routes in-process with `new Hono().route("/workflow/nodes",
    createRemoteNodeHubRoutes())` + app.fetch. Import createRemoteNodeHubRoutes from
    ../src/workflow/remoteNodeHub.ts even though it does not exist yet — that missing import IS the
    intended RED cause for the route tests. Do not implement remoteNodeHub.ts or store functions in
    this task. Never log or assert on the raw Authorization header value. Do not pass --parallel.
    Commit with: `test(workflow): add failing remote-node hub suite and migrations 003/004` including
    trailers `Story: ST-088` and `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`.
  </action>
  <verify>
    <automated>docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/workflow-remote-node-hub.test.ts 2>&1 | tee /tmp/w0.txt; grep -Eiq "module not found.*remoteNodeHub|cannot find module.*remoteNodeHub" /tmp/w0.txt</automated>
    <automated>docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/workflow-migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The migration discovery/shape/ledger tests PASS (003/004 discovered in order; both tables and
      all constraints present in the real `workflow` schema; second run skips 1..4).
    - Every route/auth/events test FAILS, and the failure is the missing remoteNodeHub.ts route
      (module resolution naming remoteNodeHub.ts), not a database or assertion-framework error.
      The verify grep asserts THAT specific cause — a bare "error|fail" match would pass on a
      TypeScript syntax error or a DB outage and prove nothing.
    - workflow-migrations.test.ts and workflow-mvp-e2e.test.ts pass with their migration-set
      expected values updated to 001..004 and no assertion shape weakened (<migration_set_fanout>).
    - The suite uses withScratchSchema with a test_hub_ prefix and drops its schema in finally; the
      production `workflow` schema is never dropped; no --parallel flag is present.
    - No test logs or asserts on a raw bearer/Authorization value.
    - Commit carries both required trailers.
  </acceptance_criteria>
  <done>The hub suite exists and is red for the intended reason; migrations 003/004 exist, are
  discovered in order, create the documented tables and constraints in the `workflow` schema, and a
  second run skips 1..4; and the migration-set fan-out is absorbed in both affected suites.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: GREEN tracer — node store functions + remoteNodeHub.ts factory wired through index.ts, end-to-end</name>
  <precondition>Task 1 recorded decision **A**. If Task 1 recorded **B**, HALT: this task does not
  execute under the current single-column schema and requires a replan (see Task 1).</precondition>
  <read_first>
    - server/src/workflow/store.ts (existing exported store functions; holds the `sql` handle — add node functions here)
    - server/src/workflow/api.ts (createWorkflowApi factory L253; toHttpError L142 — exported; readJson L223 and withErrorMapping L240 are NOT exported)
    - server/src/workflow/schema.ts (checksumOf sha256 pattern L72-80 — reuse for sha256Hex)
    - server/src/auth.ts (requireApiKey shape — mirror the signature but fail with 401 on absent header, never throw/exit)
    - server/tests/workflow-boundary.test.ts (ALLOWED_IMPORTS L94; "only store.ts holds the database handle" L221; schema-qualification regex ~L271)
    - server/index.ts (L73-91 is the migration-REPORT block only — logging and Deno.exit, no `app`; the HTTP mount seam is `if (workflowFeatureEnabled())` at L1174, with the /api/workflow middleware at L1208 and route at L1233. Mount the node routes at the L1174 seam.)
    - <research_corrections> and <locked_contract> above
  </read_first>
  <behavior>
    Turn every red test from Task 2 green (except the auth-rejection / boot-isolation edges owned by
    Plan 02) with no test edits that weaken assertions:
    - registerExecutionNode: returns the same node_id for the same bearer digest, exactly one row,
      concurrency-safe.
    - ingestRunEvents: inserts each submitted event once, ignores duplicate (node_id, client_seq),
      and the acknowledgement covers every submitted client_seq.
    - the raw bearer never reaches any column, any log line, or any response body.
  </behavior>
  <action>
    In server/src/workflow/store.ts add (using the existing `sql` handle, every statement
    workflow.-qualified). **Transaction binding:** the established pattern is
    `sql.begin(async (tx: SqlExecutor) => ...)` (store.ts:170, 234, 341) — every statement inside
    the callback must use the `tx` tag, NOT `sql`. Writing ``sql`...` `` inside a `sql.begin`
    callback silently escapes the transaction and destroys the concurrency guarantee below.
    **Parameter style:** store.ts uses postgres.js tagged templates with `${}` interpolation; the
    `$hash` / `$id` / `$seqs` below are pseudo-parameters denoting bind values, not literal syntax.
    - `upsertExecutionNode({ bearerTokenHash, hostname, platform }): Promise<{ node_id }>` — inside a
      single `sql.begin`: first `UPDATE workflow.execution_nodes SET last_seen_at = now(), status =
      'active' WHERE bearer_token_hash = $hash RETURNING node_id`; if no row, `INSERT INTO
      workflow.execution_nodes (bearer_token_hash, hostname, platform) VALUES (...) ON CONFLICT
      (bearer_token_hash) DO NOTHING RETURNING node_id`; if still no row (lost the concurrent race),
      `SELECT node_id FROM workflow.execution_nodes WHERE bearer_token_hash = $hash`. Do NOT use
      `ON CONFLICT ... DO UPDATE SET` (breaks the boundary schema-qualification regex — see
      <research_corrections>).
    - `findExecutionNode(nodeId): Promise<{ node_id, bearer_token_hash } | null>` and
      `nodeOwnsBearer(nodeId, bearerTokenHash): Promise<boolean>` implemented as
      `SELECT node_id FROM workflow.execution_nodes WHERE node_id = $id AND bearer_token_hash = $hash`
      (parameterized; ownership binding lives in SQL, not a JS compare).
    - `ingestRunEvents(nodeId, events): Promise<void>` — one `sql.begin` looping
      `INSERT INTO workflow.run_events (node_id, client_seq, event_type, payload) VALUES (...) ON
      CONFLICT (node_id, client_seq) DO NOTHING`.
    - `acknowledgeSeqs(nodeId, seqs): Promise<{ client_seq, event_id }[]>` —
      `SELECT event_id, client_seq FROM workflow.run_events WHERE node_id = $id AND client_seq = ANY($seqs)`.
    Create server/src/workflow/remoteNodeHub.ts exporting `createRemoteNodeHubRoutes(): Hono` and
    `validateNodeBearer(req: Request): Response | null`. Imports: Hono/Context (npm), z (npm),
    `node:crypto` timingSafeEqual, `toHttpError` from `./api.ts`, node functions from `./store.ts`.
    Do NOT import `../db.ts`. Define a local `sha256Hex` (crypto.subtle SHA-256, hex — copy schema.ts
    pattern), a local error wrapper mirroring api.ts withErrorMapping, and a local readJson. Routes:
    - POST /register: validateNodeBearer structural check; Zod-validate { hostname?, platform? };
      hash presented bearer; call upsertExecutionNode; return { node_id } as 201.
    - POST /:node_id/events: validateNodeBearer structural check; Zod-validate node_id is a uuid and
      { events: [{ client_seq int>=0, event_type non-empty, payload? }] } bounded .min(1).max(500);
      confirm the node exists via findExecutionNode (FK/absence → 404 via toHttpError); ingestRunEvents;
      return { acknowledged: acknowledgeSeqs(...) } as 200. (The bearer↔node ownership rejection and
      MEMORY_API_KEY isolation are Plan 02; here the happy path uses the node's own returned node_id.)
    validateNodeBearer returns a 401 Response when the Authorization header is absent OR the extracted
    bearer does not match the required format `^[0-9a-f]{64}$` (32-byte / 64-lowercase-hex
    high-entropy bearer, see <bearer_input_contract>), and null otherwise; it NEVER throws and NEVER
    calls Deno.exit (an absent node bearer means "no node
    configured", not "server misconfigured"). Never log the Authorization value or raw bearer.
    In server/index.ts, inside the existing `if (workflowFeatureEnabled())` block at **:1174** —
    the HTTP mount seam, alongside `app.route("/api/workflow", createWorkflowApi())` at :1233 —
    mount:
    `app.use("/workflow/nodes/*", async (c, next) => { const denied = validateNodeBearer(c.req.raw);
    if (denied) return denied; await next(); });` then `app.route("/workflow/nodes",
    createRemoteNodeHubRoutes());`.
    **NOT the `if (workflowBootstrap.enabled)` block at :74.** That block is module top-level, holds
    only console logging and `Deno.exit(1)`, and runs before `const app = new Hono()` at :1132 —
    mounting there is a ReferenceError on `app`, not a style preference. Both predicates derive from
    `FEATURE_WORKFLOW === "true"` (bootstrap.ts:41-43, :63), so the boot-isolation property is
    identical either way; only :1174 is reachable.
    Path prefix is /workflow/nodes/* (NOT /api/workflow/...), deliberately: the operator/agent
    credential middleware at :1208 is scoped to `/api/workflow/*` and must NOT apply to the node
    surface, which authenticates with its own per-node bearer. Do NOT
    touch startupValidation.ts. Commit: `feat(workflow): add remote-node registration and event
    ingestion hub` with trailers `Story: ST-088` and
    `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`.
  </action>
  <verify>
    <automated>docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/workflow-remote-node-hub.test.ts && docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/workflow-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every register/upsert/concurrency/events/duplicate-ack test from Task 2 passes; no assertion was
      weakened to make it pass.
    - workflow-boundary.test.ts passes unchanged: remoteNodeHub.ts does not import ../db.ts; store.ts
      SQL is fully workflow.-qualified; no DO UPDATE SET token trips the regex.
    - A second register with the same bearer returns the same node_id with exactly one row; a
      duplicate (node_id, client_seq) yields one row and a complete acknowledgement.
    - Only a 64-char hex digest is persisted for the bearer; the raw bearer never appears in a column,
      log line, or response body.
    - A bearer not matching `^[0-9a-f]{64}$` is rejected with 401 BEFORE hashing and persists no row
      (high-entropy floor, <bearer_input_contract>).
    - index.ts mounts the routes only inside the `if (workflowFeatureEnabled())` block at :1174;
      startupValidation.ts is unchanged.
    - Commit carries both required trailers.
  </acceptance_criteria>
  <done>Register→persist→ingest→acknowledge works end-to-end on the happy path, is committed, and the
  workflow boundary suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node → hub (`/workflow/nodes/*`) | Untrusted machine-to-machine request over Tailscale crosses into hub persistence. |
| request → workflow store | User-controlled bearer, node_id, and event payload cross into SQL and the run_events table. |
| optional workflow module → server boot | Presence/absence of a node bearer must not influence whether the server starts. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | register handler / execution_nodes | high | mitigate | Persist only sha256Hex(bearer); never log Authorization or any raw bearer; return only node_id (Task 3). |
| T-02-04 | Tampering | store.ts SQL under AGE-polluted search_path | high | mitigate | Every table/index/DML is `workflow.`-qualified in 003/004 and store.ts; enforced by workflow-boundary.test.ts schema-qualification test (Task 3). |
| T-02-05 | Denial of Service | events batch / payload size | medium | mitigate | Zod `events` array `.min(1).max(500)`, `event_type` non-empty, `client_seq` int>=0 (Task 3). Payload-size ceiling hardened in Plan 02. |
| T-02-06 | Tampering | duplicate replay of (node_id, client_seq) | medium | mitigate | UNIQUE(node_id, client_seq) + `ON CONFLICT DO NOTHING`; acknowledgement re-derived by SELECT so duplicates are acknowledged, not errored (Tasks 2/3). |
| T-02-07 | Denial of Service | optional-module boot coupling | high | mitigate | Node bearer validated only in the handler/middleware; startupValidation.ts untouched; routes mounted inside `if (workflowFeatureEnabled())` at index.ts:1174 (Task 3). Boot-isolation control in Plan 02. |
| T-02-SC | Tampering | Option B bcrypt dependency (npm:bcryptjs) | high | mitigate | Option B is a STOP-and-replan branch, NOT an in-plan install: the Task 1 checkpoint:decision halts execution before Task 2 and returns a schema/dependency replan (PO approval + deliberate deno.lock refresh + package-legitimacy verification of [ASSUMED] npm:bcryptjs). Option A (default execution path) adds no dependency. |
| T-02-08 | Spoofing | register handler / bearer format | high | mitigate | Deterministic SHA-256 is sound only for a high-entropy bearer; register rejects any bearer not matching `^[0-9a-f]{64}$` (32-byte / 256-bit floor) with 401 before hashing, and the operational `openssl rand -hex 32` generation command is documented (Task 2 RED control, <bearer_input_contract>). |

Cross-node injection (T-02-02) and bearer timing comparison (T-02-03) are high/medium threats whose
rejection paths are owned by Plan 02; this plan's events handler binds writes to the node's own
returned node_id and introduces no cross-node read surface.
</threat_model>

<verification>
- Quick (per task): `docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/workflow-remote-node-hub.test.ts`
- Wave (this plan): `docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read --allow-write=/tmp --allow-run=deno,git tests/workflow-remote-node-hub.test.ts tests/workflow-migrations.test.ts tests/workflow-mvp-e2e.test.ts tests/workflow-store.test.ts tests/workflow-boundary.test.ts`
  (workflow-mvp-e2e.test.ts is in the wave because this plan now edits it — <migration_set_fanout>.
  It spawns a real server, hence `--allow-run=deno,git` and `--allow-write=/tmp`.)
- Confirm tests execute inside `mcp-test` (bind-mounts ./server:/app, docker-compose.yml L89-110); never `mcp`. Never pass `--parallel`.
</verification>

<success_criteria>
- Migrations 003/004 apply cleanly onto a 002 database and create both tables with all documented
  constraints; a second `runWorkflowMigrations()` applies nothing and skips 1..4 (ledger-owned
  idempotency is a SKIP — loud failure is the raw-DDL property, see <schema_gate>).
- The migration-set fan-out is absorbed: workflow-migrations.test.ts and workflow-mvp-e2e.test.ts
  assert 001..004 with no assertion shape weakened.
- POST /workflow/nodes/register upserts one node per bearer digest and returns node_id (NODE-01,
  including the resolved repeat-registration and concurrency probes).
- POST /workflow/nodes/:node_id/events persists ownership-attributed events, is duplicate-idempotent,
  and acknowledges every submitted client_seq (NODE-02).
- workflow-boundary.test.ts and the existing workflow suites remain green.
</success_criteria>

<output>
Create `.planning/phases/02-remote-node-identity-hub/02-01-SUMMARY.md` when done.
</output>
