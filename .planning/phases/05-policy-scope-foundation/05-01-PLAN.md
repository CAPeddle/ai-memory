---
phase: 05-policy-scope-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/policyScope.ts
  - server/db/007_policy_scope.sql
  - server/db/schema.sql
  - server/src/migrate.ts
  - server/tests/migrations.test.ts
  - server/tests/policy-scope-vocabulary.test.ts
  - server/tests/policy-scope-migration.test.ts
  - server/index.ts
  - server/src/consolidationWorker.ts
autonomous: true
requirements: [POLICY-01, POLICY-02]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every row in public.thoughts (pre-existing and newly inserted) carries a non-null policy_scope value from {personal, corporate, mixed, public} after the 007 migration runs against db-test — zero rows exist with NULL or an out-of-vocabulary value (POLICY-01/POLICY-02, ROADMAP SC #1)."
    - "Inserting a thought with an out-of-vocabulary policy_scope value (e.g. 'bogus') fails at the database layer with a CHECK-constraint violation, not silently accepted (POLICY-01)."
    - "Inserting a thought into public.thoughts without a policy_scope value fails with a NOT-NULL violation after migration 007 has run — there is no table-level DEFAULT an INSERT can silently rely on (POLICY-01, Pattern 1)."
    - "capture_thought's INSERT and consolidationWorker.ts's promote INSERT both continue to succeed post-migration: capture_thought supplies the D-05 interim value 'corporate' explicitly; promote carries the source shard's own policy_scope forward rather than hardcoding one (Pitfall 3)."
    - "A fresh docker compose --profile test up -d --build and a long-running, already-migrated db-test converge on the identical policy_scope column/constraint shape — 007_policy_scope.sql, schema.sql, migrate.ts's detectBootstrapVersions(), and migrations.test.ts's version-list assertions all move together in this one change (Pitfall 4)."
    - "shared/policyScope.ts exports POLICY_SCOPES, PolicyScope, isPolicyScope, and PolicyScopeSchema, importable from server/src/*.ts and server/tests/*.ts via ../../shared/policyScope.ts, with no new dependency (zod is already pinned)."
  artifacts:
    - "shared/policyScope.ts"
    - "server/db/007_policy_scope.sql"
    - "server/db/schema.sql (edited: policy_scope column added to CREATE TABLE public.thoughts)"
    - "server/src/migrate.ts (edited: detectBootstrapVersions() v7 branch)"
    - "server/tests/migrations.test.ts (edited: version-list assertions at lines 27, 41, 100, 107 extended)"
    - "server/tests/policy-scope-vocabulary.test.ts"
    - "server/tests/policy-scope-migration.test.ts"
    - "server/index.ts (edited: capture_thought INSERT supplies policy_scope)"
    - "server/src/consolidationWorker.ts (edited: promote INSERT carries policy_scope forward)"
  key_links:
    - "shared/policyScope.ts's PolicyScope union type is the type Plan 05-02's server/src/policyScope.ts imports for withPolicyScope<T>(scope: PolicyScope, ...) — Plan 05-02 cannot compile without this file existing first, which is why it is a separate, later wave."
    - "server/db/007_policy_scope.sql's final column/constraint shape must byte-match what server/db/schema.sql declares inline in CREATE TABLE public.thoughts — a mismatch means a freshly-built db-test (schema.sql path) and a long-running upgraded db (migration path) diverge silently, and the auto-generated constraint name Postgres gives schema.sql's inline CHECK must be the exact name the migration's guarded ADD CONSTRAINT checks for."
    - "server/src/migrate.ts's new v7 detectBootstrapVersions() branch and server/tests/migrations.test.ts's FOUR hardcoded version-list locations (lines 27, 41, 100, 107 — not just one) must change together or the migration framework's own test suite reds on every run."
    - "capture_thought's ON CONFLICT (content_fingerprint) DO UPDATE SET list (server/index.ts:547-558) must NOT gain policy_scope — the existing row's scope always wins on a re-capture, never merged/widened."
  assumptions:
    - "[flagged-unverified] POLICY-01: the automated edge-probe returned 'unclassified' for this requirement — no additional edge shape was identified beyond the 4-value closed-vocabulary match. Case-sensitivity, whitespace, and empty-string handling are assumed sufficiently covered by the z.enum + CHECK constraint pairing and exercised by policy-scope-vocabulary.test.ts's reject-cases, but were not independently probed by the deterministic engine."
    - "[flagged-unverified] POLICY-02: the automated edge-probe returned 'unclassified' for this requirement — no additional edge shape beyond 'backfill NULL rows to corporate, then enforce NOT NULL' was identified. Concurrent-write-during-migration ordering (a capture_thought INSERT racing the backfill UPDATE mid-migration) is assumed safe because migrate.ts applies each file inside a single sql.begin() transaction (server/src/migrate.ts:48-58), but this was not independently verified under concurrent load."
  prohibitions:
    - "[flagged-unverified] The interim policy_scope value supplied at the capture_thought and consolidationWorker.ts promote INSERT sites (D-05) must not be implemented as a table-level DEFAULT on thoughts.policy_scope — only as an explicit per-statement value — because a DEFAULT would silently reintroduce the exact 'permissive default mints permissive rows at any forgetful INSERT site' hazard POLICY-01's no-DEFAULT design exists to prevent."
---

<objective>
Make POLICY-01 and POLICY-02 true: every thought (new and pre-existing) carries a non-null,
closed-vocabulary `policy_scope` value, and the two existing `INSERT INTO thoughts` call sites keep
working post-migration by supplying one explicitly. This plan does not touch SQL enforcement
(DECISION-01, Plan 05-02) or any retrieval path (Phase 6) — it only makes the column and its
guarantees exist.

Purpose: Phase 6-9 cannot build scope-aware retrieval filtering against a column that doesn't exist
yet, backfills inconsistently, or that two of the four production write sites would break the moment
it goes `NOT NULL`. This plan closes that gap first, foundationally, before any enforcement mechanism
(Plan 05-02) or retrieval filtering (Phase 6) is built on top of it.

Output: `shared/policyScope.ts` (closed-vocabulary module), `server/db/007_policy_scope.sql`
(migration), matching edits to `schema.sql`/`migrate.ts`/`migrations.test.ts` (the migration-lockstep
cluster, RESEARCH.md Pitfall 4), two new test files, and updated `capture_thought`/
`consolidationWorker.ts` `INSERT` statements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-policy-scope-foundation/05-CONTEXT.md
@.planning/phases/05-policy-scope-foundation/05-RESEARCH.md
@.planning/phases/05-policy-scope-foundation/05-PATTERNS.md
@.planning/phases/05-policy-scope-foundation/05-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire the policy_scope column through every migration-lockstep surface, end-to-end, proven against real db-test</name>
  <files>server/db/007_policy_scope.sql, server/db/schema.sql, server/src/migrate.ts, server/tests/migrations.test.ts, server/tests/policy-scope-migration.test.ts</files>
  <read_first>
    - server/db/workflow/001_workflow_schema.sql:32-49 — the exact shipped `policy_scope text NOT NULL CHECK (policy_scope IN ('personal','corporate','mixed','public'))` column shape and its no-DEFAULT rationale comment (lines 32-36), the verbatim precedent this phase's column mirrors
    - server/db/006_tags_replace_profile.sql (full file, 36 lines) — the "standalone idempotent delta" header convention AND the `DO $$ BEGIN IF EXISTS (...) THEN ... END IF; END $$;` guarded-DDL pattern (needed below for the CHECK-constraint idempotency guard)
    - server/db/schema.sql:13-49 — current `CREATE TABLE public.thoughts` block, to add the new column inline in the correct place
    - server/src/migrate.ts:108-197 — `detectBootstrapVersions()`, specifically the v6 branch (lines 178-196), the exact probe/insert idiom to mirror for the new v7 branch
    - server/tests/migrations.test.ts (full file, 315 lines) — ALL FOUR hardcoded version-array occurrences (lines 27, 41, 100, 107 — confirmed by `grep -n "\[1, 2" server/tests/migrations.test.ts`) plus the filename array (lines 28-35), and the migration-006 test pair (lines 120-237) to mirror for the new policy-scope-migration.test.ts
  </read_first>
  <precondition>The docker test profile is up: `docker compose --profile test up -d --wait` has completed and `db-test`/`mcp-test` are healthy. Migrations run against `db-test`, never the dev database.</precondition>
  <behavior>
    - Test: a pre-existing row with no policy_scope value is backfilled to 'corporate' when migration 007 runs (D-02).
    - Test: post-migration, INSERT with policy_scope='bogus' is rejected by the CHECK constraint.
    - Test: post-migration, INSERT omitting policy_scope entirely is rejected by NOT NULL.
    - Test: detectBootstrapVersions() marks version 7 applied only when the policy_scope column exists.
    - Test (regression): migrations.test.ts's four hardcoded version-list assertions all include 7 after a real runMigrations() call against db-test.
  </behavior>
  <action>
    Create `server/db/007_policy_scope.sql`. Header comment names ST-082/v1.1, cites the no-DEFAULT
    rationale verbatim from `001_workflow_schema.sql:32-36` (a permissive default silently mints
    permissive rows at any INSERT site that forgets the column), and D-02's corporate-not-personal
    backfill rationale. Statements, in this exact order: `ALTER TABLE public.thoughts ADD COLUMN IF
    NOT EXISTS policy_scope text;` then `UPDATE public.thoughts SET policy_scope = 'corporate' WHERE
    policy_scope IS NULL;` then `ALTER TABLE public.thoughts ALTER COLUMN policy_scope SET NOT NULL;`
    then a `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname =
    'thoughts_policy_scope_check') THEN ALTER TABLE public.thoughts ADD CONSTRAINT
    thoughts_policy_scope_check CHECK (policy_scope IN ('personal', 'corporate', 'mixed', 'public'));
    END IF; END $$;`. The guard is NOT optional — comment explaining exactly why: on a database
    bootstrapped from `schema.sql` (see next step), this exact constraint name already exists
    (Postgres auto-names an unnamed inline column CHECK as `<table>_<column>_check`), and
    `server/tests/migrations.test.ts`'s "migration 003 repair probe reapplies missing recall_queries
    when v3 is already recorded" test (lines 273-314) applies migrations 004-007 via the non-bootstrap
    path against a database that already has `schema.sql`'s cumulative shape — an unguarded `ADD
    CONSTRAINT` there throws "constraint ... already exists", which propagates through
    `migrate.ts`'s catch block into `Deno.exit(1)` and breaks that pre-existing test.

    Edit `server/db/schema.sql`: inside `CREATE TABLE public.thoughts (...)`, add a new labeled block
    directly after the `tags` line (line 30) and before the `-- Tier lifecycle` comment (line 32): a
    `-- Policy scope (ST-082 / v1.1 Policy-Scope Isolation)` comment plus `policy_scope text NOT NULL
    CHECK (policy_scope IN ('personal', 'corporate', 'mixed', 'public')),` — no DEFAULT, matching the
    migration's final shape exactly. This inline CHECK auto-generates the constraint name
    `thoughts_policy_scope_check`, which is why the migration's guard above checks for that exact
    name.

    Edit `server/src/migrate.ts`: append a new v7 probe block inside `detectBootstrapVersions()`
    (after the existing v6 block, before the function's closing brace at line 197): probe
    `information_schema.columns` for `table_schema = 'public' AND table_name = 'thoughts' AND
    column_name = ${"policy_scope"}`; if found, `INSERT INTO schema_migrations (version, filename)
    VALUES (7, '007_policy_scope.sql') ON CONFLICT (version) DO NOTHING` — mirror the exact
    query/insert idiom used by every prior branch (e.g. `needsEmbeddingColumn`, lines 121-131).

    Edit `server/tests/migrations.test.ts` at all FOUR locations: append `, 7` to the version arrays
    at lines 27 (`[1, 2, 3, 4, 5, 6]` -> `[1, 2, 3, 4, 5, 6, 7]`) and 41 (same change); append `,
    "007_policy_scope.sql"` to the filename array at lines 28-35; append `, 7` to line 107's
    `afterPartial` array (same `[1, 2, 3, 4, 5, 6]` -> `[1, 2, 3, 4, 5, 6, 7]` change, because
    `runMigrations()` at line 102 now also applies the new 007 file). Line 100's `afterBootstrapProbe`
    array is a DIFFERENT, non-obvious change: it currently reads `[1, 2, 4, 5]` (v3 and v6 are
    deliberately excluded there because that test dropped `recall_queries` first) — but v7's new
    bootstrap-detection condition depends ONLY on the `policy_scope` column existing, not on
    `recall_queries` at all, so v7 gets marked applied in that same probe regardless. Change line 100
    to `[1, 2, 4, 5, 7]`, not `[1, 2, 3, 4, 5, 6, 7]` — do not conflate this with the other three
    locations.

    Create `server/tests/policy-scope-migration.test.ts`. Import `assertEquals`, `assertRejects` from
    `https://deno.land/std@0.224.0/assert/mod.ts`; import `sql` from `../src/db.ts`; import
    `detectBootstrapVersions` from `../src/migrate.ts`; import `requireTestDatabase` from
    `./_helpers/testDatabaseGuard.ts`. Call `await requireTestDatabase();` at module top, exactly as
    `server/tests/migrations.test.ts:14` does — this file mutates schema state. Two `Deno.test` blocks
    mirroring the migration-006 pair (lines 120-237) in shape, adapted for 007:

    (a) "migration 007: backfills pre-existing rows to corporate and enforces NOT NULL + CHECK" —
    inside `sql.begin(async (tx) => {...})`: `await tx\`ALTER TABLE public.thoughts DROP COLUMN IF
    EXISTS policy_scope\`` (undoes the current schema.sql-declared state to simulate a genuine
    pre-migration database); insert one `thoughts` row supplying only `content` and
    `content_fingerprint` (a unique fingerprint via `crypto.randomUUID()`), capturing its `id`; run
    `await tx.unsafe(migration)` where `migration` is the file content read via
    `Deno.readTextFile(new URL("../db/007_policy_scope.sql", import.meta.url))`; assert the captured
    row's `policy_scope` now reads `'corporate'`. Then assert BOTH negative cases — each wrapped in
    its own `tx.savepoint(async (sp) => { ... })`, NOT two bare `assertRejects` calls back-to-back:
    a Postgres error inside `sql.begin()` aborts the whole transaction (error 25P02, "current
    transaction is aborted"), so a second `assertRejects` after the first failed INSERT would pass
    for the wrong reason — it never reaches the NOT NULL check at all, it just observes the
    already-aborted transaction. `await assertRejects(() => tx.savepoint((sp) => sp\`INSERT ...
    policy_scope: 'bogus' ...\`))` (CHECK violation — `sql.savepoint`'s callback rejecting
    auto-issues `ROLLBACK TO SAVEPOINT`, per porsager/postgres's documented savepoint semantics,
    leaving the outer `tx` usable for what follows); then, separately, `await assertRejects(() =>
    tx.savepoint((sp) => sp\`INSERT ... (omitting policy_scope) ...\`))` (NOT NULL violation) — this
    second savepoint only runs, and only proves what it claims to prove, because the first one
    rolled back cleanly instead of aborting the whole transaction. Finally `throw new
    Error("rollback migration 007 fixture")`, caught by `.catch((err) => { if ((err as
    Error).message !== "rollback migration 007 fixture") throw err; })` — identical rollback-fixture
    idiom to the 006 test.

    (b) "migration 007 bootstrap: only marks v7 when policy_scope column exists" — mirrors lines
    198-237 exactly: inside `sql.begin(...)`, `DROP COLUMN IF EXISTS policy_scope`, `DROP TABLE IF
    EXISTS schema_migrations` + recreate it, call `detectBootstrapVersions(tx)`, assert `SELECT
    version FROM schema_migrations WHERE version = 7` returns zero rows; then `ADD COLUMN
    policy_scope text` (plain, no NOT NULL/CHECK needed — this test only probes column existence),
    call `detectBootstrapVersions(tx)` again, assert the row `{ version: 7, filename:
    "007_policy_scope.sql" }` now exists; rollback-fixture pattern identical to (a). After the
    `sql.begin(...).catch(...)` block, restore canonical state exactly as 006's companion test does:
    `await sql\`DROP TABLE IF EXISTS schema_migrations\`; await runMigrations();`.

    [BLOCKING — this migration must actually run against a live database before this task is
    complete, not merely exist as a .sql file on disk.] Run the commands in `<verify>` below. The
    first `migrations.test.ts` `Deno.test` ("migration framework: bootstrap, apply, and rollback",
    lines 16-75) drops `schema_migrations` and calls `runMigrations()` for real (not inside a rollback
    transaction) — this IS the real migration applying for real against the real `db-test` container,
    and its own assertions (now extended to include version 7) are the proof `schema_migrations`
    records the new version.
  </action>
  <verify>
    <automated>docker compose --profile test up -d --wait &amp;&amp; docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/policy-scope-migration.test.ts tests/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/db/007_policy_scope.sql` contains no `DEFAULT` clause anywhere for `policy_scope`.
    - `server/db/007_policy_scope.sql`'s `ADD CONSTRAINT` statement is wrapped in a `DO $$ ... IF NOT EXISTS (SELECT ... FROM pg_constraint ...)` guard.
    - `server/db/schema.sql`'s `CREATE TABLE public.thoughts` block contains a `policy_scope` column with `NOT NULL` and the 4-value `CHECK`, and no `DEFAULT`.
    - `server/src/migrate.ts`'s `detectBootstrapVersions()` contains a new probe for `column_name = 'policy_scope'` that inserts version 7 on match.
    - `server/tests/migrations.test.ts` has `7` appended at all four locations (lines ~27, ~41, ~100, ~107 pre-edit) and `"007_policy_scope.sql"` appended to the filename array — verify line 100's array is `[1, 2, 4, 5, 7]`, distinct from the other three which are `[1, 2, 3, 4, 5, 6, 7]`.
    - `server/tests/policy-scope-migration.test.ts` exists with both the backfill/constraint test and the bootstrap-detection companion test, and calls `requireTestDatabase()` at module top.
    - `docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/policy-scope-migration.test.ts tests/migrations.test.ts` exits 0.
  </acceptance_criteria>
  <done>All four migration-lockstep surfaces (007 file, schema.sql, migrate.ts, migrations.test.ts) move together in one change; policy-scope-migration.test.ts and migrations.test.ts both pass against the real db-test container, proving schema_migrations records version 7 for real, not merely that the .sql file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the shared/policyScope.ts closed-vocabulary module and its unit tests</name>
  <files>shared/policyScope.ts, server/tests/policy-scope-vocabulary.test.ts</files>
  <read_first>
    - shared/tagGrammar.ts (full file, 119 lines) — the closed-vocabulary/branded-type export shape to mirror structurally (not the regex machinery — policy_scope is a closed enum, not an open grammar)
    - server/tests/parseContext.test.ts:1-5 — the exact `import { ... } from "../../shared/tagGrammar.ts";` path convention a server/tests/*.test.ts file uses to import a shared/*.ts module two directories up
    - server/deno.json — confirms `zod` is pinned at `npm:zod@4.1.13` in the `imports` map; no new dependency is added
  </read_first>
  <behavior>
    - Test: isPolicyScope('personal'|'corporate'|'mixed'|'public') => true for all four.
    - Test: isPolicyScope('professional'|'PERSONAL'|'personal '|''|'public,personal'|'mixed_scope') => false for all six near-misses.
    - Test: PolicyScopeSchema.safeParse(value).success matches isPolicyScope(value) for every value in both lists above — the type guard and the Zod schema never disagree.
  </behavior>
  <action>
    Create `shared/policyScope.ts`: `export const POLICY_SCOPES = ["personal", "corporate", "mixed",
    "public"] as const;` (this exact order and these exact four strings, matching POLICY-01 and the
    shipped `workflow.work_packets.policy_scope` CHECK list); `export type PolicyScope =
    typeof POLICY_SCOPES[number];`; `export function isPolicyScope(value: string): value is
    PolicyScope { return (POLICY_SCOPES as readonly string[]).includes(value); }`; import `z` from
    `"zod"` and `export const PolicyScopeSchema = z.enum(POLICY_SCOPES);`.

    Create `server/tests/policy-scope-vocabulary.test.ts`: import `{ POLICY_SCOPES, isPolicyScope,
    PolicyScopeSchema }` from `"../../shared/policyScope.ts"` (mirroring `parseContext.test.ts:3`'s
    import-path convention) and `assertEquals` from the std assert module. Three `Deno.test` blocks:
    (1) loop over `POLICY_SCOPES` asserting `isPolicyScope(value)` is `true` for each; (2) a table of
    six near-miss values — `"professional"` (the old ADR-012 binary-`profile` value, must not be
    confused with this new closed vocabulary), `"PERSONAL"` (case sensitivity), `"personal "`
    (trailing whitespace), `""` (empty string), `"public,personal"` (comma-joined, guards against
    treating this like a multi-value tag list), `"mixed_scope"` (partial match) — asserting
    `isPolicyScope(value)` is `false` for each; (3) loop over the union of both lists asserting
    `PolicyScopeSchema.safeParse(value).success === isPolicyScope(value)` for every value.
  </action>
  <verify>
    <automated>docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/policy-scope-vocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `shared/policyScope.ts` exports exactly `POLICY_SCOPES`, `PolicyScope`, `isPolicyScope`, `PolicyScopeSchema`.
    - All 4 `POLICY_SCOPES` values pass both `isPolicyScope` and `PolicyScopeSchema.safeParse(...).success`.
    - All 6 near-miss values in the reject table fail both `isPolicyScope` and `PolicyScopeSchema.safeParse(...).success`.
    - `server/tests/policy-scope-vocabulary.test.ts` contains at least 3 `Deno.test` blocks.
    - `deno test ... tests/policy-scope-vocabulary.test.ts` exits 0.
  </acceptance_criteria>
  <done>shared/policyScope.ts's closed-vocabulary validation is proven correct for the 4 accept cases and 6 reject cases, including the old ADR-012 'professional' value and case/whitespace/list-confusion near-misses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update capture_thought and consolidationWorker's promote INSERT to supply policy_scope (Pitfall 3)</name>
  <files>server/index.ts, server/src/consolidationWorker.ts, server/tests/policy-scope-migration.test.ts</files>
  <read_first>
    - server/index.ts:486-605 — capture_thought's full registerTool block; the INSERT at lines 525-546 and the ON CONFLICT ... DO UPDATE SET clause at lines 547-558
    - server/src/consolidationWorker.ts:100-146 — promote(), the INSERT at lines 122-134
    - .planning/phases/05-policy-scope-foundation/05-CONTEXT.md — D-05 (interim scope for new writes)
    - .planning/phases/05-policy-scope-foundation/05-RESEARCH.md — Pitfall 3 (both insert-site fixes and the carry-forward rationale for promote)
  </read_first>
  <behavior>
    - Test: a literal INSERT matching capture_thought's updated column list, with policy_scope='corporate', succeeds and reads back 'corporate'.
    - Test: a literal INSERT matching promote's updated SELECT-based column list, sourced from a 'personal'-scoped shard, reads back policy_scope='personal' on the resulting row (not 'corporate' — proving carry-forward, not a hardcoded literal).
  </behavior>
  <action>
    Edit `server/index.ts`'s `capture_thought` INSERT (lines 525-546): add `policy_scope` to the
    column list (place it after `tags,` and before `content_fingerprint,`) and add the literal string
    `'corporate'` at the corresponding position in `VALUES` (D-05's interim value — a hardcoded
    literal, not a bind parameter from caller input; `capture_thought` has no caller-supplied scope
    parameter this milestone, CAPTURE-01 being deferred to v2). Do NOT modify the `ON CONFLICT
    (content_fingerprint) DO UPDATE SET` clause at lines 547-558 — confirm it still does not reference
    `policy_scope` anywhere, so a re-capture of existing content never changes or merges its scope.

    Edit `server/src/consolidationWorker.ts`'s `promote()` INSERT (lines 122-134): add `policy_scope`
    to the column list (after `tags,`) AND add `policy_scope` to the `SELECT` list (after `project,
    tags,`, i.e. `project, tags, policy_scope,`) so the promoted wiki row inherits the SAME
    `policy_scope` as the source shard identified by `WHERE id = ${shardId}`. Do NOT hardcode a
    literal here, unlike `capture_thought` — a wiki promoted from a `personal`-scoped shard must not
    silently become `corporate`.

    Extend `server/tests/policy-scope-migration.test.ts` (created in Task 1) with a third `Deno.test`
    block: "post-migration: capture_thought and consolidation-promote insert shapes satisfy
    policy_scope NOT NULL". Using plain `sql` (not a rollback transaction — this test proves the REAL,
    currently-shipping call-site shapes against the real committed schema, not a synthetic
    migration-transition state): issue a literal INSERT replicating capture_thought's exact
    (post-edit) column list with `policy_scope: 'corporate'`, assert it succeeds and reads back
    `'corporate'`; issue a literal INSERT for a source shard with `policy_scope: 'personal'`, then a
    second literal INSERT ... SELECT replicating promote's exact (post-edit) column/SELECT list
    sourced `FROM thoughts WHERE id = <the shard's id>`, assert the resulting wiki row's
    `policy_scope` reads `'personal'` (not `'corporate'`); clean up both rows and the shard row with
    an explicit `DELETE FROM thoughts WHERE id = ANY(...)` at the end of the test.

    After these edits, restart `mcp-test` so its already-running server process (bind-mounted, but
    NOT hot-reloaded — Deno does not reload a running long-lived process on file change) picks up the
    `capture_thought`/`promote` code changes before the full suite runs: `docker compose --profile
    test restart mcp-test`. The full suite includes `tests/e2e.test.ts`'s real HTTP calls to
    `capture_thought` via `mcpCall`, which independently exercises this exact code path against the
    real running server.
  </action>
  <verify>
    <automated>docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/policy-scope-migration.test.ts &amp;&amp; docker compose --profile test restart mcp-test &amp;&amp; docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read --allow-write=/tmp --allow-run=deno,git tests/</automated>
  </verify>
  <acceptance_criteria>
    - `server/index.ts`'s `capture_thought` INSERT column list includes `policy_scope`; the corresponding `VALUES` position holds the literal `'corporate'`.
    - `grep -c policy_scope` over `server/index.ts` lines 547-558 (the `ON CONFLICT` SET clause) returns `0`.
    - `server/src/consolidationWorker.ts`'s promote() INSERT column list AND SELECT list both include `policy_scope`; the SELECT list selects it FROM the source row (not a literal constant).
    - `policy-scope-migration.test.ts`'s new third test passes, proving both insert shapes succeed post-migration and that promote carries scope forward (personal in -&gt; personal out).
    - Full test suite (`tests/` with all four grants) exits 0 after `docker compose --profile test restart mcp-test`.
  </acceptance_criteria>
  <done>Both existing INSERT INTO thoughts call sites supply an explicit policy_scope value (capture_thought: 'corporate' interim; promote: carried forward from the source shard), closing Pitfall 3's post-migration hard-fail risk, proven by both a direct SQL-shape test and the full suite (including e2e.test.ts's real capture_thought HTTP calls against the restarted mcp-test container).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool caller (single `MEMORY_API_KEY`) -> `capture_thought` -> `thoughts.policy_scope` | The point where a value that every later phase will trust for isolation first gets written; today it is always the hardcoded interim `'corporate'` (D-05), not caller-declared (CAPTURE-01 is v2). |
| `consolidationWorker.ts` background worker -> `thoughts.policy_scope` (promote) | An internal, non-caller-facing write path; must carry the source row's own scope forward rather than invent one. |
| Migration runner (`migrate.ts`) -> `public.thoughts` DDL | Runs automatically at server boot (`server/index.ts:58`) against whatever `DATABASE_URL` resolves to — a real production-schema-altering trust boundary, not just a test concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | `thoughts.policy_scope` column (007 migration + schema.sql) | high | mitigate | `NOT NULL` + `CHECK` with no `DEFAULT` (Pattern 1), enforced by both the migration and schema.sql; zero rows can exist without an explicit valid scope value, closing the default-allow gap before any Phase 6 enforcement lands. |
| T-05-01-02 | Tampering | `capture_thought`'s `ON CONFLICT ... DO UPDATE` clause (server/index.ts:547-558) | high | mitigate | Task 3's acceptance criteria assert `policy_scope` is never added to the `SET` list — the existing row's scope always wins, never merged/widened by a re-capture. |
| T-05-01-03 | Information Disclosure | `capture_thought` / `consolidationWorker.ts` promote INSERT sites | high | mitigate | Both INSERT sites explicitly supply `policy_scope` (interim `'corporate'` at capture, carried-forward from source shard at promote) rather than relying on a table DEFAULT — closes Pitfall 3's post-migration hard-fail/scope-omission risk. |
| T-05-01-04 | Denial of Service | `007_policy_scope.sql`'s `ADD CONSTRAINT` step, re-applied via the non-bootstrap migration path (e.g. the "migration 003 repair probe" test scenario) | medium | mitigate | Constraint-add wrapped in a `DO $$ IF NOT EXISTS (... pg_constraint ...)` guard so re-running the migration content does not throw "constraint already exists" and abort the entire migration runner (`Deno.exit(1)`). |
| T-05-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan — `zod` and `postgres` are both already-pinned dependencies (RESEARCH.md's Package Legitimacy Audit: "Not applicable"). |

**Canon-referral note:** a candidate prohibition considered and dropped — "policy_scope must not silently coerce/normalize invalid input (case-folding, trimming) instead of rejecting it outright" — maps to ASVS V5 generic input-validation canon and is already structurally closed by `z.enum`'s exact-match behavior (Pattern 2), so it was not minted as a project-specific prohibition per the canon-referral rule.
</threat_model>

<verification>
1. `docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read tests/policy-scope-migration.test.ts tests/policy-scope-vocabulary.test.ts tests/migrations.test.ts` — all green.
2. `docker compose --profile test exec mcp-test deno test --frozen --allow-net --allow-env --allow-read --allow-write=/tmp --allow-run=deno,git tests/` — full suite green after `docker compose --profile test restart mcp-test`.
3. `grep -c policy_scope` over `server/index.ts`'s `ON CONFLICT` SET clause (lines 547-558) returns `0`.
4. `server/db/schema.sql` and `server/db/007_policy_scope.sql` declare the identical `policy_scope` column/constraint shape (no DEFAULT, same CHECK list).
</verification>

<success_criteria>
- Every row in `public.thoughts` on `db-test` has a valid, non-null `policy_scope` after this plan's migration runs — proven against the real container, not asserted from the .sql file alone.
- Both existing `INSERT INTO thoughts` call sites (`capture_thought`, `consolidationWorker.ts` promote) succeed post-migration without relying on a table `DEFAULT`.
- The migration framework's own regression suite (`migrations.test.ts`) passes with the new version 7 correctly reflected at all four hardcoded locations.
</success_criteria>

## Artifacts this phase produces

Phase-wide inventory (this plan's contributions marked **[05-01]**; Plan 05-02's are marked **[05-02]** for cross-reference — every plan in this phase carries the same roll-up so either can be checked independently).

**New files**
- **[05-01]** `shared/policyScope.ts` — `POLICY_SCOPES`, `PolicyScope`, `isPolicyScope`, `PolicyScopeSchema`
- **[05-01]** `server/db/007_policy_scope.sql`
- **[05-01]** `server/tests/policy-scope-vocabulary.test.ts`
- **[05-01]** `server/tests/policy-scope-migration.test.ts`
- **[05-02]** `server/src/policyScope.ts` — `withPolicyScope()` (committed, unwired)
- **[05-02]** `server/tests/policy-scope-rls-spike.test.ts`
- **[05-02]** `docs/design/adr/ADR-018-policy-scope-sql-enforcement-mechanism.md`

**Modified files**
- **[05-01]** `server/db/schema.sql` — `policy_scope` column added to `CREATE TABLE public.thoughts`
- **[05-01]** `server/src/migrate.ts` — `detectBootstrapVersions()` gains a v7 branch
- **[05-01]** `server/tests/migrations.test.ts` — version-list assertions extended to include 7 (four locations)
- **[05-01]** `server/index.ts` — `capture_thought` INSERT supplies `policy_scope = 'corporate'`
- **[05-01]** `server/src/consolidationWorker.ts` — `promote()` INSERT carries `policy_scope` forward from the source shard

**New migration version:** 7 (`007_policy_scope.sql`) **[05-01]**

**New environment variables:** none this phase.

**New CLI surface:** none this phase.

**New ADR:** ADR-018 (mechanism decision, trust boundary, visibility matrix) **[05-02]**

<output>
Create `.planning/phases/05-policy-scope-foundation/05-01-SUMMARY.md` when done.
Commit each task separately with a Conventional Commit carrying a `Story: ST-082` trailer.
</output>
